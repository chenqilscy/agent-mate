# AgentMate 已关闭 Issues：WB-500～WB-546

> 本文件由 `scripts/archive_issues.py` 维护。每条记录保留原始元数据、完整正文和稳定锚点；不要手工拆回单文件。

共 46 条终态记录。

| ID | 状态 | 严重度 | 领域 | 标题 |
|---|---|---|---|---|
| [WB-500](#wb-500) | ✅ | P1 | fullstack | Console 任务详情缺少 Run 过程、交付与验收投影 |
| [WB-501](#wb-501) | ✅ | P1 | fullstack | Console 缺少 Local Agent 设备就绪状态与任务选路入口 |
| [WB-502](#wb-502) | ✅ | P1 | fullstack | Console WorkItem 缺少有门禁的一次性自动执行策略 |
| [WB-503](#wb-503) | ✅ | P2 | fullstack | Console 项目缺少 Run 效率、交付质量、成本与设备分析 |
| [WB-504](#wb-504) | ✅ | P2 | backend | Agent 对话缺少受限的 WorkItem Sprint 计划字段更新工具 |
| [WB-505](#wb-505) | ✅ | P1 | backend | WorkItem 自动执行策略配置与人工验收状态发生回归 |
| [WB-506](#wb-506) | ✅ | P2 | fullstack | 项目执行分析的窗口口径、活跃排队与子任务下钻不正确 |
| [WB-507](#wb-507) | ✅ | P1 | frontend | Console 源码功能未进入 Server 实际服务的生产构建 |
| [WB-508](#wb-508) | ✅ | P1 | frontend | 新 Console 遇到旧 Server 设备投影时 Local Agent 页面崩溃 |
| [WB-509](#wb-509) | ✅ | P2 | frontend | Console 再次残留 Ant Design 弃用属性并持续输出运行时警告 |
| [WB-510](#wb-510) | ✅ | P3 | backend | 执行分析验收窗口依赖非单调墙钟导致测试偶发失败 |
| [WB-511](#wb-511) | ✅ | P2 | frontend | Console 项目任务抽屉残留 Ant Design 6 弃用组件与属性 |
| [WB-512](#wb-512) | ✅ | P2 | frontend | Console 项目任务抽屉将评论表单嵌套在任务编辑表单内 |
| [WB-513](#wb-513) | ✅ | P2 | ui | Console 项目概览混入完整执行分析导致信息密度过高 |
| [WB-514](#wb-514) | ✅ | P2 | ui | Console 项目能力配置缺少目录信息与配置反馈 |
| [WB-515](#wb-515) | ✅ | P1 | fullstack | Console 项目连接器配置仍读取已退役的 NP_CONNS 目录 |
| [WB-516](#wb-516) | ✅ | P2 | ui | Console 任务编辑的协作与执行交付区域脱离页签分组 |
| [WB-517](#wb-517) | ✅ | P1 | fullstack | 独立 App 业务面与 Server Workspace 重叠且缺少桌面伴侣收缩路径 |
| [WB-518](#wb-518) | ✅ | P1 | frontend | Desktop Companion 首页仍复制 Server Workspace 业务工作台 |
| [WB-519](#wb-519) | ✅ | P1 | fullstack | Server Workspace 缺少原子发起执行与 Desktop Companion 会话交接 |
| [WB-520](#wb-520) | ✅ | P1 | frontend | Desktop Companion 仍可新建 Run 并复制 Server 任务上下文 |
| [WB-521](#wb-521) | ✅ | P1 | frontend | Desktop Companion 仍保留文件与知识聚合业务入口 |
| [WB-522](#wb-522) | ✅ | P1 | backend | 正式桌面 sidecar 未进入最小 Local Agent Core 模式 |
| [WB-523](#wb-523) | ✅ | P1 | frontend | App 完整发布构建被计划事件与工作台状态类型错误阻断 |
| [WB-524](#wb-524) | ✅ | P1 | misc | 发布回归契约漂移且 main 分支推送不触发 CI |
| [WB-525](#wb-525) | ✅ | P1 | misc | 真实 SQLite 备份未被仓库忽略规则覆盖 |
| [WB-526](#wb-526) | ✅ | P2 | backend | WorkItem 计划工具实现未进入设备能力注册表 |
| [WB-528](#wb-528) | ✅ | P2 | frontend | Desktop WebView 未配置 Content Security Policy |
| [WB-529](#wb-529) | ✅ | P2 | ui | Desktop 设置仍展示不可用的快捷键与支持入口 |
| [WB-530](#wb-530) | ✅ | P1 | backend | 安装版 Core 启动后保留的设备管理 API 全部失效 |
| [WB-531](#wb-531) | ✅ | P1 | frontend | Tauri CSP 静默阻断非 loopback HTTP Server |
| [WB-532](#wb-532) | ✅ | P1 | frontend | Console 交接回归门禁绑定旧实现细节并阻断全量测试 |
| [WB-533](#wb-533) | ✅ | P2 | frontend | 打开执行记录失败被静默吞掉导致路由误判成功 |
| [WB-534](#wb-534) | ✅ | P1 | backend | Server Console 缺少浏览器安全响应头基线 |
| [WB-535](#wb-535) | ✅ | P2 | frontend | Desktop Home 窄屏次要操作触控区域不足 |
| [WB-536](#wb-536) | ✅ | P2 | misc | Server-first 完成度矩阵滞后于已交付能力 |
| [WB-537](#wb-537) | ✅ | P2 | frontend | 会话恢复关键失败路径缺少可执行行为测试 |
| [WB-538](#wb-538) | ✅ | P1 | backend | 兼容 sidecar 仍挂载已退役业务 API 面 |
| [WB-539](#wb-539) | ✅ | P1 | fullstack | Console 将长效 Bearer 令牌持久化在 localStorage |
| [WB-540](#wb-540) | ✅ | P1 | frontend | Markdown 清洗依赖 DOMPurify 命中 XSS 安全公告 |
| [WB-541](#wb-541) | ✅ | P2 | backend | MCP 传递依赖与 FastAPI Starlette 版本约束冲突 |
| [WB-542](#wb-542) | ✅ | P1 | backend | 协作成员无法上传既有项目资产且审计归错账号 |
| [WB-543](#wb-543) | ✅ | P1 | backend | 并发首批注册可产生多个平台管理员 |
| [WB-544](#wb-544) | ✅ | P1 | backend | 登录限流组合键可被轮换用户名或来源绕过 |
| [WB-545](#wb-545) | ✅ | P1 | backend | SSO 首账号注册可导致平台永久没有管理员 |
| [WB-546](#wb-546) | ✅ | P1 | backend | 公共 webhook 与 relay 可在校验前无界缓冲请求体 |

---

<!-- issue-record:start {"id":"WB-500","number":500,"source":"WB-500-console-work-item-execution-projection.md","title":"Console 任务详情缺少 Run 过程、交付与验收投影","severity":"P1","area":"fullstack","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-500"></a>

## WB-500 · Console 任务详情缺少 Run 过程、交付与验收投影

<details>
<summary>原始元数据</summary>

```yaml
id: WB-500
title: Console 任务详情缺少 Run 过程、交付与验收投影
severity: P1
area: fullstack
status: fixed
origin: 既有实现
files:
  - server/routers/work_items.py:373
  - console/src/api.ts:381
  - console/src/components/project/ProjectWorkspace.tsx:1059
  - docs/agentmate-server-first-架构设计.md:167
created: 2026-08-11
```

</details>

### 问题

Server 已能按 WorkItem 返回关联 Run、执行状态、产物和验收记录，App 也已使用该投影；Console 项目工作台仍只展示计划与协作字段，缺少同一 WorkItem 的执行过程、结果、错误和验收入口。

### 触发场景

管理者在 Console 打开已交给 Local Agent 的任务，无法判断它是排队、执行、等待授权、失败还是已待验收，也无法查看产物。

### 影响

P1。Console 无法完成“管理任务并观察执行”的基本闭环，App 与 Console 对同一任务呈现出不一致事实。

### 建议修法

在 Server 提供可分页的 WorkItem Run + Delivery 读模型，Console 任务详情增加执行区：最新状态、真实计划/步骤、设备、耗时、待用户动作、结果、产物、错误和验收。共用 App 的状态语义，仅展示 Server 已接收和脱敏内容。

### 验证

- 同一 WorkItem 在 App 和 Console 显示同一 Run 状态、终态和产物。
- 排队、等待授权、失败、重试和已验收可区分，无计划时不伪造百分比。
- Viewer 只读；本机私密路径、凭据和未上传输出不出现。

### 处理记录

- 2026-08-11：根因确认。Server 的 WorkItem delivery 端点虽能返回关联 Run 与产物，但此前直接返回完整 Run 行，未形成适合 Console 的共享读模型；Console 也没有对应 API、类型与任务详情执行区，因此管理端看不到排队原因、真实计划、事件、设备、交付与验收事实。
- 2026-08-11：Server 将 delivery 改为稳定分页的字段白名单投影，补充 queue context、最近领取设备、结构化计划、允许共享的 Run 事件、产物校验与验收记录；明确排除 `request_snapshot`、`permission_snapshot`、checkpoint 等本机执行细节，并对事件中的本机路径及凭据样式文本做脱敏。
- 2026-08-11：Console 任务抽屉新增「Local Agent 执行与交付」区域，按真实状态展示排队/执行/等待用户/暂停/恢复/失败/待验收/已验收，展示计划步骤、最近事件、设备、耗时、模型用量、错误、不可变产物下载和有权限的整批验收入口；无计划时明确显示空态，不伪造进度百分比。
- 2026-08-11：自动验证通过：`server/tests/test_wb500_console_execution_projection.py` 与既有 `test_wb461_server_work_item_execution.py` 共 5 个用例通过；Console `npx tsc --noEmit` 与 `npx vite build --config vite.console.config.ts` 通过。
- 2026-08-11：真实页面验证通过。使用隔离 Server/SQLite、真实 Run 协议事件和真实对象存储产物，在 Console 完成任务详情读取、产物下载入口呈现以及「验收全部产物并完成任务」状态闭环；深色、浅色、780px、390px 均无横向溢出，浏览器控制台无错误。Viewer 只读、事件白名单、路径/凭据脱敏与分页由服务端回归用例覆盖。
- 2026-08-11：边界保留。Console 只观察并验收 Server 已接收的共享事实；等待回答或授权时仍提示执行负责人回到 App 处理，不把远程 Console 变成本机工具控制面。

<!-- issue-record:end WB-500 -->

---

<!-- issue-record:start {"id":"WB-501","number":501,"source":"WB-501-console-local-agent-routing.md","title":"Console 缺少 Local Agent 设备就绪状态与任务选路入口","severity":"P1","area":"fullstack","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-501"></a>

## WB-501 · Console 缺少 Local Agent 设备就绪状态与任务选路入口

<details>
<summary>原始元数据</summary>

```yaml
id: WB-501
title: Console 缺少 Local Agent 设备就绪状态与任务选路入口
severity: P1
area: fullstack
status: fixed
origin: 既有实现
files:
  - server/routers/run_protocol.py:92
  - server/run_protocol_store.py:420
  - server/routers/work_items.py:413
  - console/src/api.ts:331
  - docs/agentmate-server-first-架构设计.md:220
created: 2026-08-11
```

</details>

### 问题

Server Run 协议已支持多设备注册、心跳、能力匹配、容量和 `target_device_id`，但 Console 没有设备列表、实际 readiness 或在任务/自动化中指定执行设备的产品入口。

### 触发场景

同一账号注册了多台 Local Agent，其中只有一台具备目标 working copy 或私有连接器。管理者无法在 Console 选择该设备，也无法事前看到不兼容原因。

### 影响

P1。多设备协议能力不可用，后台任务可能长时间排队、选错设备或在执行后才暴露缺失能力。

### 建议修法

增加账号设备投影，展示设备名、在线、协议/App 版本、工具/Skill/连接器 readiness、容量和最近错误。任务和 Automation 使用 `specific | any_compatible`，并先固定 `execution_owner_id`；MVP 只允许选择该 owner 的已验证设备。

### 验证

- 两个独立 Local Agent 设备可同时注册，Console 显示真实心跳和能力差异。
- `specific` 仅由指定设备领取；`any_compatible` 只从同 owner 兼容设备中选路。
- 离线、撤销、不兼容、缺能力、容量满和写锁冲突都返回可解释原因。
- 多进程共用同一 SQLite/工作目录不被当成支持的多节点模式。

### 处理记录

- 2026-08-11：根因确认。Server 已有设备身份、心跳、能力匹配、租约容量与 `target_device_id`，但 `/api/devices` 只返回注册表原始行，Automation 也始终写入空目标；Console 因而既没有可操作的设备就绪投影，也不能把执行固定到某台设备。
- 2026-08-11：Server 设备投影新增验证、在线、核心协议兼容性、并行/驻留容量、最近失败与 `ready / busy / offline / incompatible / unverified / revoked` 就绪状态；继续由现有 queue context 对离线、撤销、能力不匹配、容量满、等待确认和工作区写锁给出真实原因。
- 2026-08-11：Automation 新增持久化的 `routing_mode` 与 `target_device_id`。`specific` 只接受执行 owner 名下已验证且未撤销的设备，`any_compatible` 保持空目标；调度创建 Run 时写入稳定路由，执行 owner 仍固定为自动化 owner，不因设备选择改变。
- 2026-08-11：Console 新增 Local Agent 管理页，展示设备身份、真实心跳、协议/App/平台版本、能力与工具、容量、最近错误并支持撤销；自动化新增“任一在线兼容设备 / 指定 Local Agent”选择与保存回读，并明确提示多副本必须使用独立数据目录和工作副本，不支持共享 SQLite 或同一可写工作区的多进程模式。
- 2026-08-11：自动验证通过。`test_wb501_device_routing` 覆盖两台设备的 specific/any-compatible 领取、设备投影及未验证目标拒绝；连同自动化调度、设备协议、排队原因和 schema migration 共 20 个用例通过。Console `npx tsc --noEmit` 与 `npx vite build` 通过。
- 2026-08-11：真实页面验证通过。隔离 Server/SQLite 中注册一台在线和一台离线设备，Console 正确显示就绪、离线、心跳、并行/驻留容量和工具能力；在自动化表单选择指定设备并保存后，列表稳定回读该设备。深浅主题切换正常，1280px 页面无横向溢出。浏览器只出现仓库既有的 Ant Design deprecated API 开发警告，无业务请求或渲染异常。

<!-- issue-record:end WB-501 -->

---

<!-- issue-record:start {"id":"WB-502","number":502,"source":"WB-502-work-item-auto-execution-policy.md","title":"Console WorkItem 缺少有门禁的一次性自动执行策略","severity":"P1","area":"fullstack","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-502"></a>

## WB-502 · Console WorkItem 缺少有门禁的一次性自动执行策略

<details>
<summary>原始元数据</summary>

```yaml
id: WB-502
title: Console WorkItem 缺少有门禁的一次性自动执行策略
severity: P1
area: fullstack
status: fixed
origin: 既有实现
files:
  - server/automation_scheduler.py:145
  - server/routers/work_items.py:413
  - console/src/pages/AutomationsPage.tsx:216
  - docs/agentmate-server-first-架构设计.md:237
created: 2026-08-11
```

</details>

### 问题

Console 已有定时、interval 和 Webhook Automation，但这些 fire 创建的 Run 不绑定 WorkItem。WorkItem 本身没有执行 owner、设备选路、能力门禁、幂等触发和自动重试策略，无法安全表达“这个任务就绪后由 Local Agent 自动执行一次”。

### 触发场景

管理者在 Console 创建任务并希望它自动执行；现在只能另建一条 Automation，执行结果不会自然进入该任务的 Delivery/验收闭环。

### 影响

P1。如果用布尔开关直接自动运行，会遗漏依赖、重复 Run、设备能力、后台权限和人工验收边界。

### 建议修法

为 WorkItem 增加结构化执行策略：`manual | auto`、execution owner、`specific | any_compatible`、目标设备、必需能力、超时/token/重试/通知和最小预授权。只在任务/Sprint/依赖/设备/readiness 门禁通过时幂等创建一个 Run，成功后进入人工验收。

### 验证

- 相同 WorkItem 版本的重复触发只创建一个 active Run。
- 依赖未完成、Sprint 未激活、设备离线/不兼容或高风险权限缺失时不启动，并有真实可观察原因。
- 重试创建带 `retry_of` 的新 Run；成功不自动把 WorkItem 置为 `done`。
- 与周期性 Automation 的语义、历史和幂等键明确分离。

### 处理记录

- 2026-08-11：根因确认。现有周期/Webhook Automation 只能按独立自动化定义创建 Run，WorkItem 没有执行 owner、设备路由、能力与权限门禁，也没有任务版本级幂等键和独立重试链，无法安全表达一次性交付。
- 2026-08-11：Server 新增持久化 WorkItem 执行策略与后台扫描器。策略包含 `manual / auto`、执行 owner、`specific / any_compatible`、目标设备、必需能力、超时、尝试次数、退避、token 上限、通知策略和最小预授权；策略状态和阻塞码直接挂到 WorkItem 投影。
- 2026-08-11：执行门禁覆盖任务状态、依赖、活动 Sprint、执行 owner 项目权限、设备验证/归属/在线/兼容/容量、同项目写锁与高风险权限。相同策略版本使用 `work-item-auto:<id>:v<version>:attempt:<n>` 幂等键，重复触发复用 active Run；失败重试创建带 `retry_of` 的新 Run。
- 2026-08-11：Run 继续使用既有 `projexec` 协议和 Delivery/产物验收链；执行成功只进入待验收状态，不自动把 WorkItem 标为 `done`，人工验收后策略才显示已验收。其状态、历史和幂等命名空间与周期性 Automation 完全分离。
- 2026-08-11：Console 任务执行面板新增一次性自动执行策略，支持模式、设备路由、目标节点、超时和重试配置，并显示真实的排队、运行、等待重试、门禁阻塞、待验收与已验收状态。
- 2026-08-11：自动验证通过。WB-502、WB-500、WB-501 与 schema migration 共 12 个用例通过，覆盖重复触发、依赖/Sprint/设备/权限门禁、带 `retry_of` 的重试以及成功后人工验收边界；Console `npx tsc --noEmit` 通过。
- 2026-08-11：真实页面验证通过。隔离 Server/SQLite 与 Console 中配置指定离线 Local Agent，任务详情稳定回读自动执行开关、指定设备、超时、最多尝试次数和“目标 Local Agent 已离线”门禁原因；深浅主题下策略面板均可见可操作，未产生虚假 Run。

<!-- issue-record:end WB-502 -->

---

<!-- issue-record:start {"id":"WB-503","number":503,"source":"WB-503-console-project-execution-analytics.md","title":"Console 项目缺少 Run 效率、交付质量、成本与设备分析","severity":"P2","area":"fullstack","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-503"></a>

## WB-503 · Console 项目缺少 Run 效率、交付质量、成本与设备分析

<details>
<summary>原始元数据</summary>

```yaml
id: WB-503
title: Console 项目缺少 Run 效率、交付质量、成本与设备分析
severity: P2
area: fullstack
status: fixed
origin: 既有实现
files:
  - console/src/pages/OverviewPage.tsx:14
  - console/src/api.ts:492
  - server/project_health_service.py:1
  - docs/agentmate-server-first-架构设计.md:178
created: 2026-08-11
```

</details>

### 问题

Console 已有项目数量、健康风险、任务负载和 Sprint 燃尽，但没有基于 Server Run/Delivery 的执行效率、质量、成本和设备资源投影。

### 触发场景

管理者想回答“近 7 天哪些任务排队最久、失败最多、首次验收最差、消耗最高、因设备能力阻塞”，Console 无统一数据口径。

### 影响

P2。不阻塞单次执行，但管理者无法定量判断 Agent 交付效果、成本和运行瓶颈。

### 建议修法

建立带项目/组织权限、时间窗口和口径版本的 Server 聚合 API，首批覆盖 Run 状态、排队/执行耗时、重试/成功、阻塞原因、首次验收、返工、产物校验、token/成本和设备能力/容量/WAL。Console 提供总览、趋势和下钻到真实 WorkItem/Run 的入口。

### 验证

- 聚合值可通过同一权限范围的 WorkItem/Run/Delivery/用量明细重算。
- 时间窗口、时区、重试、取消、未定价模型和未验收交付口径有版本化说明。
- Viewer 仅能看可访问项目；对个人凭据和本机私密数据不做聚合或下钻。

### 处理记录

- 2026-08-11：根因确认。项目概览只聚合任务和健康风险，真实 Run、租约设备、交付产物、验收与用量虽已在 Server 持久化，却没有统一的项目权限、时间窗口和指标口径 API。
- 2026-08-11：Server 新增 `project-execution-v1` 项目执行分析。API 强制复用项目访问权限，支持 7/30/90 天与 IANA 时区，返回精确 `[start,end)`；取消不进入成功率，排队中 Run 不进入耗时分位数，未定价成本单独计数且按记录币种分组。
- 2026-08-11：交付质量口径覆盖产物完整性校验、已验收交付、首次成功即验收和后续返工 Run；当前队列瓶颈直接由持久化设备/租约状态计算，设备仅投影项目 Run 已使用或指定节点的就绪与容量，不暴露其他本机数据。
- 2026-08-11：分析下钻只返回项目内 WorkItem/Run 标识、状态、耗时、token、预估成本和设备显示名；不聚合凭据、提示词、消息、事件载荷、错误详情、本机路径或私密文件内容。Viewer 可读可访问项目，非成员统一返回 404。
- 2026-08-11：Console 项目概览新增执行分析面板，提供窗口切换、Run/成功率/排队 P95/token、首次验收与产物校验、成本/失败、每日趋势、当前队列瓶颈、设备容量、最慢和最高成本 Run；点击任务可打开真实任务的 Run/交付详情。
- 2026-08-11：自动验证通过。WB-503 指标/权限/窗口测试及 WB-500～502 回归共 15 项通过，Console TypeScript 检查和 Vite 生产构建通过。
- 2026-08-11：真实页面验证通过。隔离 Server/SQLite 中的 3 个 Run 可人工重算为成功率 50%、排队 P95 20 秒、180 token、USD 0.25、未定价 2、首次验收 1/1；页面正确显示离线设备瓶颈和 0/2 并行容量，并可从最慢 Run 下钻到任务真实 Run。深浅主题均正常。

<!-- issue-record:end WB-503 -->

---

<!-- issue-record:start {"id":"WB-504","number":504,"source":"WB-504-agent-work-item-sprint-update-tool.md","title":"Agent 对话缺少受限的 WorkItem Sprint 计划字段更新工具","severity":"P2","area":"backend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-504"></a>

## WB-504 · Agent 对话缺少受限的 WorkItem Sprint 计划字段更新工具

<details>
<summary>原始元数据</summary>

```yaml
id: WB-504
title: Agent 对话缺少受限的 WorkItem Sprint 计划字段更新工具
severity: P2
area: backend
status: fixed
origin: 既有实现
files:
  - backend/agent/tools.py:807
  - backend/agent/tools.py:841
  - server/routers/work_items.py:273
  - src/components/project/ProjectTaskCenter.tsx:283
  - docs/agentmate-server-first-架构设计.md:187
created: 2026-08-11
```

</details>

### 问题

App 任务中心可在创建任务时指定 Sprint，Server WorkItem API 也能更新 `sprint_id`；但 Agent 对话工具只能列出任务、更改状态和启动 Run，无法完成“把某任务加入指定 Sprint”。

### 触发场景

用户在 App 对 Agent 说“把任务 A 加入 Sprint B”，Agent 只能解释或要求用户手动操作，无受限工具调用可写回 Server。

### 影响

P2。不影响 Console 手动管理，但 App 的“个人 Agent 工作台”无法完成明确、低风险的项目计划指令。

### 建议修法

增加窄范围的 WorkItem 计划字段工具，首批仅支持 `sprint_id`（必要时同步 `milestone_id`）。参数必须来自真实项目/Sprint/WorkItem 查询；Server 复用角色、项目归属、已关闭 Sprint 只读、版本冲突和审计门禁。

### 验证

- 有权成员能通过对话把真实 WorkItem 加入开放 Sprint，Console 和 App 读回同一 `sprint_id`。
- Viewer、跨项目 ID、已关闭 Sprint、不存在任务和过期版本均拒绝且不部分写入。
- 工具轨迹与 Server 活动记录包含 actor、原 Sprint、新 Sprint 和 WorkItem 版本。

### 处理记录

- 2026-08-11：根因确认。App Agent 仅有项目任务列表、状态回写和启动 Run；任务列表不返回可供模型安全使用的 WorkItem 版本与 Sprint 真值，也没有只接受计划字段的 Server 写端点，使用通用 PATCH 会扩大授权面且无法识别并发修改。
- 2026-08-11：Server 为 WorkItem 增加单调 `version`，普通编辑和验收同步推进版本；新增仅接受 `sprint_id`、`expected_version`、`sync_milestone` 的窄范围 CAS 端点。写入在一个事务中校验项目角色、任务归属、Sprint 归属、`planned / active` 生命周期和版本，任一失败整体回滚。
- 2026-08-11：App Agent 的 `list_work_items` 现在同时返回真实任务版本、当前 Sprint/里程碑以及项目 Sprint ID、状态和里程碑；新工具 `update_work_item_planning` 要求这些真实 ID 和版本，Server 不可达时明确拒绝且不修改本地镜像，plan 模式不暴露写工具。
- 2026-08-11：成功写回同时产生 Agent 工具 trace、App SSE live WorkItem 更新和 Server `planning_updated` 活动；三处均包含 actor、原/新 Sprint、原/新里程碑及 `from_version / to_version`，App 任务 Store 合并同一 `sprint_id`、`milestone_id` 和版本。
- 2026-08-11：自动验证通过。Server 成功/Viewer/跨项目/关闭 Sprint/不存在任务/过期版本/通用编辑推进版本，以及 Agent 查询/写回/离线/409/plan-mode 共 36 项针对性回归通过；App 与 Console TypeScript 检查通过。
- 2026-08-11：真实协议验证通过。隔离 Server 中由 App Agent 工具经真实 HTTP 把版本 1 的 WorkItem 加入活动 Sprint，同步里程碑并回读版本 2；Server 活动记录完整保留 actor 与前后值。用版本 1 重放返回 409、无 trace/live 写事件，Server 任务仍保持首次成功结果，无部分写入。

<!-- issue-record:end WB-504 -->

---

<!-- issue-record:start {"id":"WB-505","number":505,"source":"WB-505-work-item-auto-policy-regressions.md","title":"WorkItem 自动执行策略配置与人工验收状态发生回归","severity":"P1","area":"backend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-505"></a>

## WB-505 · WorkItem 自动执行策略配置与人工验收状态发生回归

<details>
<summary>原始元数据</summary>

```yaml
id: WB-505
title: WorkItem 自动执行策略配置与人工验收状态发生回归
severity: P1
area: backend
status: fixed
origin: 🆕 近期改动
files:
  - server/routers/work_items.py:725
  - server/work_item_auto_scheduler.py:309
  - console/src/components/project/WorkItemExecution.tsx:68
created: 2026-08-11
```

</details>

### 问题

WB-504 插入 `PlanningUpdateBody` 时截断了 `ExecutionPolicyBody`，使超时、重试、token 与通知等字段错误归属到计划更新模型；同时自动执行扫描器仅凭 WorkItem 为 `done` 就把策略标记为 `accepted`，没有核对真实验收记录。

### 触发场景

在 Console 设置自动执行超时和最多尝试次数后，Server 静默采用默认值；或把 `todo / doing` 任务直接改为完成后，策略显示“交付已验收”，但任务没有 `work_item_acceptances` 记录。

### 影响

P1。安全执行策略无法按管理者配置生效，并会越过“成功后必须人工验收”的审计边界。

### 建议修法

恢复两个 Pydantic 请求模型的字段边界；自动策略只能在存在真实验收记录时进入 `accepted`，普通手工完成应保持可解释的非验收终态。补充经 HTTP 配置策略和手工完成任务的回归测试。

### 验证

- 经策略 API 设置超时、尝试次数、退避和 token 上限后可原样读回并用于 Run 快照。
- 没有验收记录的 `done` 任务不显示“交付已验收”。
- 真实验收后策略显示 `accepted`，成功 Run 仍先进入待验收。

### 处理记录（2026-08-11）

- 改动：恢复 `ExecutionPolicyBody` 的完整字段边界；策略仅在存在真实 `work_item_acceptances` 时进入 `accepted`，手工完成改为可解释的 `completed_without_acceptance`；验收端点立即刷新自动策略状态。
- 验证：WB-500～504、schema 与 Console 契约共 28 项测试通过；新增 HTTP 策略字段、手工完成和真实验收回归。

<!-- issue-record:end WB-505 -->

---

<!-- issue-record:start {"id":"WB-506","number":506,"source":"WB-506-project-execution-analytics-correctness.md","title":"项目执行分析的窗口口径、活跃排队与子任务下钻不正确","severity":"P2","area":"fullstack","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-506"></a>

## WB-506 · 项目执行分析的窗口口径、活跃排队与子任务下钻不正确

<details>
<summary>原始元数据</summary>

```yaml
id: WB-506
title: 项目执行分析的窗口口径、活跃排队与子任务下钻不正确
severity: P2
area: fullstack
status: fixed
origin: 🆕 近期改动
files:
  - server/project_execution_analytics.py:132
  - server/project_execution_analytics.py:217
  - console/src/components/project/ProjectWorkspace.tsx:1821
created: 2026-08-11
```

</details>

### 问题

首次验收和返工只在窗口内 Run 子集中寻找“首次成功”，会把窗口前已经成功过的任务误算为首次成功；尚未启动的排队 Run 没有排队时长，无法进入最慢列表；Console 又只把根任务传给分析面板，使子任务 Run 的下钻按钮无响应。

### 触发场景

任务在统计窗口前已有成功 Run、窗口内重试并验收时，首次验收率和返工数失真；长时间未被设备领取的 Run 不出现在最慢列表；点击绑定子任务的分析记录没有打开任务详情。

### 影响

P2。管理者会基于错误的交付质量与排队瓶颈数据作判断，部分真实明细无法下钻核实。

### 建议修法

明确并实现稳定的时间窗口 cohort：为窗口内涉及的 WorkItem 查询全量成功历史来判定真实首次成功和返工，同时按验收发生时间限定统计；为活跃排队 Run 计算截至窗口结束的当前排队时长并单独标识；传递完整任务集合完成下钻。

### 验证

- 窗口外首次成功、窗口内重试不会被算作首次成功，返工数可从明细重算。
- 窗口结束后发生的验收不会改写该窗口结果。
- 尚未启动的排队 Run 可按当前排队时长排序。
- 根任务与子任务 Run 都能打开真实任务详情。

### 处理记录（2026-08-11）

- 改动：指标升级为 `project-execution-v2`，使用全历史首次成功与成功/验收事件时间建立稳定窗口 cohort；活跃排队时长按窗口结束时刻计算但不进入 P95；Console 下钻改用完整任务集合。
- 验证：新增跨窗口返工、窗口后验收和活跃排队回归；WB-503 及关联回归通过，Console TypeScript 检查和生产构建通过。

<!-- issue-record:end WB-506 -->

---

<!-- issue-record:start {"id":"WB-507","number":507,"source":"WB-507-console-dist-release-drift.md","title":"Console 源码功能未进入 Server 实际服务的生产构建","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-507"></a>

## WB-507 · Console 源码功能未进入 Server 实际服务的生产构建

<details>
<summary>原始元数据</summary>

```yaml
id: WB-507
title: Console 源码功能未进入 Server 实际服务的生产构建
severity: P1
area: frontend
status: fixed
origin: 🆕 近期改动
files:
  - console/src/App.tsx:25
  - server/main.py:94
  - server/web/console-dist/index.html:8
created: 2026-08-11
```

</details>

### 问题

WB-500～WB-503 修改了 Console 源码并通过 Vite 构建，但提交没有包含更新后的 `server/web/console-dist`。Server 运行时直接服务该目录，因此提交树仍展示旧 Console，且不存在 Local Agents 页面 chunk。

### 触发场景

直接从已提交版本启动 Server 并打开 Console，无法访问本轮新增的任务执行、Local Agent、自动执行策略和项目执行分析功能。

### 影响

P1。源码与实际发布产物分叉，已声明完成的控制面功能在真实 Server 页面不可用。

### 建议修法

使用受控 Console 构建命令重新生成并提交 `server/web/console-dist`，校验入口引用的所有 chunk 均存在；增加构建产物一致性门禁，避免源码修改后继续提交旧 dist。

### 验证

- 提交树中的 `index.html` 引用真实存在的新 hash 资源。
- 生产构建包含 LocalAgentsPage 与更新后的 ProjectDetailPage。
- 从 Server 静态挂载访问新增路由和项目执行面板成功。

### 处理记录（2026-08-11）

- 改动：重新生成并纳入 `server/web/console-dist`；新增 `check:console-dist`，在隔离临时目录重建 Console 并逐文件校验 hash；静态入口测试同时检查引用资源存在。
- 验证：`npm run check:console-dist` 确认源码与 21 个生产文件完全一致；入口包含 LocalAgentsPage、ProjectDetailPage 与执行分析功能。

<!-- issue-record:end WB-507 -->

---

<!-- issue-record:start {"id":"WB-508","number":508,"source":"WB-508-console-local-agent-rolling-upgrade-compatibility.md","title":"新 Console 遇到旧 Server 设备投影时 Local Agent 页面崩溃","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-508"></a>

## WB-508 · 新 Console 遇到旧 Server 设备投影时 Local Agent 页面崩溃

<details>
<summary>原始元数据</summary>

```yaml
id: WB-508
title: 新 Console 遇到旧 Server 设备投影时 Local Agent 页面崩溃
severity: P1
area: frontend
status: fixed
origin: 🆕 近期改动
files:
  - console/src/pages/LocalAgentsPage.tsx:73
  - console/src/types.ts:59
created: 2026-08-11
```

</details>

### 问题

Local Agent 页面直接解引用新设备投影的 `readiness` 和 `capacity`。滚动升级期间，如果浏览器先加载新版 Console 而 Server 进程仍返回旧设备结构，页面会抛出 `Cannot read properties of undefined` 并进入错误边界。

### 触发场景

保持旧 Server 进程运行，加载新版 Console 后打开 `/local-agents`，设备记录没有 `readiness / capacity`，页面立即崩溃。

### 影响

P1。Console 与 Server 无法安全滚动升级，设备管理入口完全不可用。

### 建议修法

把新增投影字段建模为可选兼容字段，页面根据旧有 `status / authenticated_at / capabilities` 提供只读降级展示，并避免零容量或缺失能力对象的解引用。

### 验证

- 新 Console 读取旧设备投影时页面正常展示“等待 Server 更新”与保守容量。
- 新 Server 完整投影继续显示真实 readiness 和容量。
- 页面无运行时异常，深浅主题均可访问。

### 处理记录（2026-08-11）

- 改动：新增统一 Local Agent 展示兼容层，基于旧投影保守推导身份、状态与容量；Local Agent、Automation 和 WorkItem 设备选择共同复用，避免缺失字段解引用。
- 验证：真实浏览器在新版 Console + 未重启旧 Server 下复现后修复，页面显示“等待 Server 更新”和保守容量；新标签页无 TypeError，明暗主题切换正常；Console 类型检查、生产构建及 dist 一致性门禁通过。

<!-- issue-record:end WB-508 -->

---

<!-- issue-record:start {"id":"WB-509","number":509,"source":"WB-509-console-antd-deprecation-regression.md","title":"Console 再次残留 Ant Design 弃用属性并持续输出运行时警告","severity":"P2","area":"frontend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-509"></a>

## WB-509 · Console 再次残留 Ant Design 弃用属性并持续输出运行时警告

<details>
<summary>原始元数据</summary>

```yaml
id: WB-509
title: Console 再次残留 Ant Design 弃用属性并持续输出运行时警告
severity: P2
area: frontend
status: fixed
origin: 🆕 近期改动
files:
  - console/src/LoginPage.tsx:103
  - console/src/pages/CatalogPage.tsx:352
  - console/src/SkillsPage.tsx:345
  - console/src/SkillEditor.tsx:400
  - console/src/components/project/ProjectExecutionAnalytics.tsx:102
created: 2026-08-11
```

</details>

### 问题
Console 真实浏览器遍历登录、目录、用户和项目页时，Ant Design 6.5.1 持续输出 `Card bordered`、`Space direction`、`Drawer width`、`Modal maskClosable` 和 `Alert message` 弃用警告。源码仍在多个新页面与编辑器中使用这些旧属性，属于已关闭 WB-303 的新一轮复发。

### 触发场景
以平台管理员登录 `:8103`，依次打开概览、专家/连接器/技能目录、用户管理和项目详情，并打开新建或编辑抽屉；浏览器控制台稳定出现上述弃用警告，单次全路由巡检累计多条重复日志。

### 影响
开发控制台被噪声淹没，真实请求或渲染异常更难识别；这些属性在后续 Ant Design 主版本可能被移除，升级时会直接形成 UI 回归，因此按 P2 跟踪。

### 建议修法
按 Ant Design 6 当前 API 逐项迁移：`Card bordered` 改 `variant`，`Space direction` 改 `orientation`，`Drawer width` 改 `size` 或兼容尺寸方案，`Modal maskClosable` 改 `mask.closable`，`Alert message` 改 `title`。补一条 Console 源码契约测试，覆盖这些已知旧属性，避免再次复发。

### 验证
运行 Console 类型检查与生产构建；真实浏览器在明暗主题、桌面与 600px 窄屏下遍历登录、目录、用户、项目详情和抽屉，控制台不再出现上述 Ant Design 弃用警告，业务请求和页面渲染保持正常。

### 处理记录（2026-08-11）

- 根因：Console 新增页面和抽屉继续沿用 Ant Design 5/早期 6 的旧属性，但现有测试只校验入口与主题契约，没有覆盖已弃用属性，导致 WB-303 之后再次回归。
- 改动：将 `Card.bordered`、`Space.direction`、`Drawer.width`、`maskClosable`、`Alert.message` 全量迁移到 Ant Design 6 对应 API；新增源码契约测试扫描全部 Console TSX，阻止这些旧属性再次进入。
- 验证：Console TypeScript 检查、7 项入口/弃用契约测试、隔离生产构建均通过；真实浏览器遍历概览、能力目录、自动化、Local Agent、组织、用户、平台设置与真实项目详情，打开用户、技能、任务抽屉，在暗色桌面和 600px 亮色窄屏下无横向溢出；目标 `Card/Space/Drawer/Modal/Alert` 告警均为 0。
- 边界：任务抽屉另发现 `Timeline/InputNumber/List` 弃用和嵌套表单问题，已分别登记 WB-511、WB-512，未夹带到本修复。

<!-- issue-record:end WB-509 -->

---

<!-- issue-record:start {"id":"WB-510","number":510,"source":"WB-510-project-analytics-wall-clock-flake.md","title":"执行分析验收窗口依赖非单调墙钟导致测试偶发失败","severity":"P3","area":"backend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-510"></a>

## WB-510 · 执行分析验收窗口依赖非单调墙钟导致测试偶发失败

<details>
<summary>原始元数据</summary>

```yaml
id: WB-510
title: 执行分析验收窗口依赖非单调墙钟导致测试偶发失败
severity: P3
area: backend
status: fixed
origin: 🆕 近期改动
files:
  - server/project_execution_analytics.py:90
  - server/project_execution_analytics.py:139
  - server/tests/test_wb503_project_execution_analytics.py:128
created: 2026-08-11
```

</details>

### 问题
WB-503 执行分析在函数入口用第二次 `time.time()` 生成窗口上界，并用严格的 `accepted_at < window_end` 过滤验收事件。测试先以一次墙钟写入 `accepted_at`，随即用另一次墙钟构建窗口；Windows 墙钟发生毫秒级回拨时，刚写入的验收记录会短暂落在窗口上界之后，`first_pass_accepted` 从 1 变成 0。

### 触发场景
运行 `python -m unittest discover -s server/tests -p "test_*.py"`，182 项中 WB-503 的首次验收断言出现过 `1.0 != 0.0`；整模块运行也曾在第 128、169 行失败，而只运行单个测试或随后重跑整模块可以通过。失败现场数据库已有对应验收和首次成功 Run，但聚合结果的 `accepted_deliveries` 为 0，符合窗口上界瞬时回拨后的排除路径。

### 影响
全量回归存在非确定性红灯，降低发布门禁可信度；极端墙钟校正窗口内，刚验收后立即查询的真实 Console 也可能短暂显示首次验收为 0。当前可重试恢复且时间窗很短，按 P3 跟踪。

### 建议修法
让测试显式注入稳定 `now`，确保验收写入与查询窗口使用同一时间基准；生产聚合应明确事件窗口的 as-of 语义，避免依赖两个可能回拨的墙钟读数。不要简单扩大整个统计窗口或把未来事件无条件纳入，以免破坏 WB-506 的“窗口后验收不回写历史窗口”契约。

### 验证
以固定 `now` 覆盖首次验收、跨窗口返工和窗口后验收三类测试；重复运行 WB-503 模块和 Server 全量套件至少 20 次均无失败，并保留窗口后验收仍为 0 的断言。

### 处理记录（2026-08-11）

- 根因：测试种子和聚合查询分别读取墙钟，严格的 `accepted_at < window_end` 在 Windows 墙钟毫秒级回拨时会把刚写入的验收排除；测试没有使用生产函数已经提供的显式 `now` 时间缝。
- 改动：统一测试数据的分析基准 `analysis_now`，显式写入验收时间并向聚合器注入同一 as-of；重试、历史窗口和窗口后验收场景均从该基准派生。生产代码补充 as-of 语义说明，保持未来事件不回写历史窗口的既有契约。
- 验证：WB-503 模块 4 个并行分片各重复 5 次，共 20/20 通过；模块 5 项、Server 全量 183 项、Python 编译均通过，窗口后验收仍断言为 0。

<!-- issue-record:end WB-510 -->

---

<!-- issue-record:start {"id":"WB-511","number":511,"source":"WB-511-console-project-detail-antd6-deprecations.md","title":"Console 项目任务抽屉残留 Ant Design 6 弃用组件与属性","severity":"P2","area":"frontend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-511"></a>

## WB-511 · Console 项目任务抽屉残留 Ant Design 6 弃用组件与属性

<details>
<summary>原始元数据</summary>

```yaml
id: WB-511
title: Console 项目任务抽屉残留 Ant Design 6 弃用组件与属性
severity: P2
area: frontend
status: fixed
origin: 🆕 近期改动
files:
  - console/src/pages/ProjectDetailPage.tsx:943
  - console/src/pages/ProjectDetailPage.tsx:1327
  - console/src/components/project/ProjectWorkspace.tsx:1375
  - console/src/components/project/ProjectWorkspace.tsx:1467
  - console/src/components/project/WorkItemExecution.tsx:256
created: 2026-08-11
```

</details>

### 问题
WB-509 的真实浏览器复验打开项目任务详情抽屉后，控制台仍出现 Ant Design 6 的三类独立弃用告警：`Timeline.items.children` 应迁移到 `items.content`，`InputNumber.addonAfter` 应迁移到 `Space.Compact`，`List` 组件将在下一主版本移除。此前 WB-509 的问题边界只覆盖 `Card/Space/Drawer/Modal/Alert` 旧属性，现有静态防回归也未覆盖这些 API。

### 触发场景
平台管理员打开 Console 概览中的真实项目，进入含历史 Run 的任务详情抽屉；执行时间线、工时字段、评论与交付列表渲染时，浏览器控制台稳定输出上述弃用警告。

### 影响
任务详情是项目执行和验收的核心入口，持续告警会掩盖真实运行异常；相关 API 在后续 Ant Design 主版本删除后，时间线、工时输入或列表区域可能直接回归，按 P2 跟踪。

### 建议修法
按 Ant Design 6 当前 API 迁移所有项目详情路径：时间线项统一使用 `content`，工时输入用 `Space.Compact` 组合单位，列表改为受支持的 Table、Flex 或语义化 DOM；扩展 Console 源码契约测试并覆盖真实任务抽屉。

### 验证
Console 类型检查和生产构建通过；在有真实 Run 与交付的项目任务抽屉中覆盖内容、计划、更多信息和执行记录，浏览器控制台不再出现 `Timeline`、`InputNumber` 或 `List` 弃用警告，桌面与 600px 窄屏布局正常。

### 处理记录

2026-08-12：项目详情三条路径的 Timeline item 全部迁移为 `content`，工时输入改为 `Space.Compact`，执行计划与交付列表切换到共享语义化 `CompatList`；新增源码契约测试阻止 `Timeline.children`、`InputNumber.addonAfter` 和 Ant Design `List` 回归。前端 8/8、Console 类型检查与生产构建通过。浏览器控制技能因本机内核运行时资源路径缺失未能启动，因此本次不声称已取得真实浏览器控制台或 600px 截图证据；代码与构建修复已完成。

<!-- issue-record:end WB-511 -->

---

<!-- issue-record:start {"id":"WB-512","number":512,"source":"WB-512-project-task-drawer-nested-form.md","title":"Console 项目任务抽屉将评论表单嵌套在任务编辑表单内","severity":"P2","area":"frontend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-512"></a>

## WB-512 · Console 项目任务抽屉将评论表单嵌套在任务编辑表单内

<details>
<summary>原始元数据</summary>

```yaml
id: WB-512
title: Console 项目任务抽屉将评论表单嵌套在任务编辑表单内
severity: P2
area: frontend
status: fixed
origin: 🆕 近期改动
files:
  - console/src/components/project/ProjectWorkspace.tsx:985
  - console/src/components/project/ProjectWorkspace.tsx:1548
created: 2026-08-11
```

</details>

### 问题
项目任务详情抽屉以一个 Ant Design `Form` 包裹任务编辑内容，又在“任务协作”区域渲染独立评论 `Form`，最终生成 `<form>` 嵌套 `<form>` 的无效 HTML。React 在真实浏览器中明确报告 hydration/DOM nesting 错误。

### 触发场景
平台管理员打开任一可写项目的真实任务详情抽屉；只要评论输入区域渲染，浏览器控制台就出现 `form cannot be a descendant of form` 与 `form cannot contain a nested form` 两条错误。

### 影响
无效表单结构可能导致 Enter 提交命中错误处理器、任务保存与评论发送互相干扰，也会让浏览器和辅助技术对控件归属产生歧义。该入口涉及真实任务变更与评论写入，按 P2 跟踪。

### 建议修法
拆分任务编辑与评论提交的 DOM 表单边界：优先把评论表单移出外层任务 `Form`，或让其中一层使用 `component={false}` 并显式绑定独立提交按钮；同时补渲染测试，断言任务抽屉不存在嵌套 `<form>`。

### 验证
真实项目任务抽屉中分别验证任务保存、Enter 行为和评论发送；DOM 中不存在嵌套 `<form>`，控制台无 React nesting/hydration 错误，评论和任务保存仍各自只触发一次请求。

### 处理记录（2026-08-11）
- 改动：`console/src/components/project/ProjectWorkspace.tsx` 将任务协作区移出任务字段 `Form`，评论继续使用独立表单，并归入“协作与交付”页签。
- 验证：Console 类型检查、生产构建和 dist 一致性检查通过；真实任务抽屉 DOM 的 `form form` 数量为 0，空评论回车不产生评论、不启用或触发任务保存，浏览器 warning/error 为 0。
- commit：随本次修复提交。

<!-- issue-record:end WB-512 -->

---

<!-- issue-record:start {"id":"WB-513","number":513,"source":"WB-513-project-overview-analytics-tab.md","title":"Console 项目概览混入完整执行分析导致信息密度过高","severity":"P2","area":"ui","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-513"></a>

## WB-513 · Console 项目概览混入完整执行分析导致信息密度过高

<details>
<summary>原始元数据</summary>

```yaml
id: WB-513
title: Console 项目概览混入完整执行分析导致信息密度过高
severity: P2
area: ui
status: fixed
origin: 🆕 近期改动
files:
  - console/src/components/project/ProjectWorkspace.tsx:1822
  - console/src/pages/ProjectDetailPage.tsx:69
created: 2026-08-11
```

</details>

### 问题

`ProjectOverview` 在项目健康、任务摘要、整体进度、里程碑和近期活动之外，直接渲染完整的
`ProjectExecutionAnalyticsPanel`。该分析面板还包含统计窗口说明、四项 KPI、交付质量、成本与失败、每日趋势、
当前排队瓶颈、Local Agent 设备负载、最慢 Run 和最高成本 Run，导致“概览”首屏和纵向内容被运行数据占据，
项目摘要与深度分析缺少清晰的信息边界。

### 触发场景

打开 Console → 项目 → 任一存在 Run 数据的项目 → “概览”，会在项目摘要中直接看到整块 Agent 执行分析；
用户需要滚动很长距离才能浏览概览的任务进度、里程碑和近期活动。

### 影响

P2。不会导致数据错误，但项目概览的扫描效率明显下降，执行指标和项目状态混在同一标签中，也使后续指标扩展
持续放大页面密度。

### 建议修法

在项目一级标签中新增“数据”，将现有 `ProjectExecutionAnalyticsPanel` 原样迁入该标签；“概览”仅保留项目健康、
任务摘要、整体进度、里程碑和近期活动。新标签应支持 `?tab=` 深链、浏览器前进/后退、刷新恢复，并沿用现有
Ant Design 标签和响应式布局，不复制或重写指标口径。

### 验证

- “概览”不再请求或渲染 Agent 执行分析，仍展示健康、任务统计、整体进度、里程碑和近期活动。
- “数据”标签展示原有完整执行分析，窗口切换、刷新和任务下钻保持可用。
- `?tab=analytics` 可直达，刷新及浏览器前进/后退能恢复正确标签；非法 `tab` 仍回退概览。
- Console 类型检查与生产构建通过；桌面和窄宽下无横向溢出，明暗主题均可读。

### 处理记录（2026-08-11）

- 改动：为 Console 项目工作区新增“数据”一级标签和 `analytics` 深链，将现有完整执行分析从“概览”迁入；
  概览保留项目健康、任务摘要、整体进度、里程碑和近期活动，数据页隐藏无关的“新建任务”操作。
- 验证：`npx tsc --noEmit`、`pnpm build:console`、隔离工作树中的 `pnpm check:console-dist` 均通过；
  在真实 Console 项目页验证桌面暗色、桌面浅色、760px 窄宽均无横向溢出，概览不显示执行分析，数据页完整显示
  13 个真实 Run 的分析；`?tab=analytics` 刷新恢复、浏览器前进/后退和非法参数回退均正确。
- commit：待本次提交。

<!-- issue-record:end WB-513 -->

---

<!-- issue-record:start {"id":"WB-514","number":514,"source":"WB-514-project-capability-config-design.md","title":"Console 项目能力配置缺少目录信息与配置反馈","severity":"P2","area":"ui","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-514"></a>

## WB-514 · Console 项目能力配置缺少目录信息与配置反馈

<details>
<summary>原始元数据</summary>

```yaml
id: WB-514
title: Console 项目能力配置缺少目录信息与配置反馈
severity: P2
area: ui
status: fixed
origin: 🔁 既有实现
files:
  - console/src/pages/ProjectDetailPage.tsx:1372
  - console/src/styles.css:135
created: 2026-08-11
```

</details>

### 问题
Console 项目“设置”中的“项目能力配置”把连接器、专家和技能目录压缩成三个仅显示名称的多选框。目录原有的说明、分类、工具范围和配置状态都被丢弃，也没有已选摘要、目录加载失败、历史能力失效、未保存变更或保存结果反馈，无法支撑项目管理员做可靠的能力编排。

### 触发场景
项目 Owner/Admin 打开 Console 项目详情的“设置”标签，为项目填写默认指令并选择连接器、专家和技能；项目已有能力、目录项较多、目录项被停用，或当前用户/项目处于只读状态时尤为明显。

### 影响
管理员只能根据名称猜测能力用途，容易把“目录可选”误解为“本机已就绪”，也难以发现项目仍引用已失效的历史能力；保存前后缺少明确反馈，配置决策和排障成本较高，按 P2 跟踪。

### 建议修法
保留现有 `instruction/connectors/experts/skills` PATCH 契约，增加有搜索、说明、分类/工具标签、选中状态和失效项提示的能力选择器；补充默认指令、能力摘要、知识库入口、目录加载与错误状态、未保存/已同步反馈，以及 Owner/Admin、Viewer、归档项目的明确只读说明。连接器只展示目录与配置要求，不伪造 Local Agent 实际就绪状态，也不在项目配置中保存凭据。

### 验证
Console 类型检查与生产构建通过；验证目录加载、搜索、选择、取消、保存和 API 回读，失效历史项可见且可移除；Owner/Admin 可编辑，Viewer 和归档项目明确只读；明暗主题及约 760px 窄屏下无溢出、焦点和选中状态清晰，知识库入口能跳转到既有标签。

### 处理记录

- **根因**：旧实现把三类 Server 目录数据立即压缩为 `{value,label}`，再交给原生多选框，目录说明、分类、工具、配置要求等真实字段在渲染前已经丢失；同时没有为加载、历史失效引用、权限/归档、脏状态和保存结果建立界面状态。
- **修复范围**：在 `ProjectDetailPage.tsx` 增加目录字段归一化和受控能力卡片选择器，支持搜索、说明、分类/工具标签、连接器配置要求、选中态与历史失效项移除；重组默认指令、装载摘要、知识库入口、加载/错误/只读提示与保存状态，仍只提交既有 `instruction/connectors/experts/skills` 字段。`styles.css` 增加基于 Console token 的明暗主题、键盘焦点、选中态和 900/640px 响应式布局。
- **防回归**：新增 `server/tests/test_wb514_project_capability_config.py`，约束丰富目录字段、搜索与历史/权限边界、原 PATCH 字段和响应式样式，不再使用已弃用的 `Spin.tip`。
- **验证结果**：Console 类型检查、生产构建和 `check:console-dist` 通过；WB-514/WB-515 及既有项目管理共 10 条测试通过。真实登录项目中目录显示连接器 6、专家 13、技能 6，搜索空态正确；选择“本地便签”保存后刷新仍为选中，随后取消保存并回读恢复为原始 0 项。知识库“管理”跳转到 `tab=knowledge`；760×900 暗色/亮色均无横向溢出，摘要为双列、保存区纵排；新页面 warning/error 为 0。
- **边界**：目录卡的“目录内置/需本机配置”只描述定义与配置要求；具体设备是否安装、授权和兼容仍由 Local Agent 权威状态决定，本页不保存任何凭据。

<!-- issue-record:end WB-514 -->

---

<!-- issue-record:start {"id":"WB-515","number":515,"source":"WB-515-project-connector-catalog-category.md","title":"Console 项目连接器配置仍读取已退役的 NP_CONNS 目录","severity":"P1","area":"fullstack","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-515"></a>

## WB-515 · Console 项目连接器配置仍读取已退役的 NP_CONNS 目录

<details>
<summary>原始元数据</summary>

```yaml
id: WB-515
title: Console 项目连接器配置仍读取已退役的 NP_CONNS 目录
severity: P1
area: fullstack
status: fixed
origin: 🔁 既有实现
files:
  - console/src/pages/ProjectDetailPage.tsx:1629
  - server/routers/projects.py:97
created: 2026-08-11
```

</details>

### 问题
Server-first 连接器权威目录已使用 `CONN_DEFS`，当前运行库中有 6 条启用定义；但 Console 项目设置仍请求 `NP_CONNS`，服务端项目 loadout 校验也只接受 `NP_CONNS`。运行库没有该旧分类，导致连接器配置恒为 0 项，即使前端自行展示新目录，保存时也会被服务端判为未知连接器。

### 触发场景
项目 Owner/Admin 打开 Console 项目详情的“设置”标签并进入连接器能力配置，或向 `PATCH /projects/{id}` 提交 `CONN_DEFS` 中存在的连接器名称。

### 影响
项目默认连接器无法发现、无法选择和保存，连接器 loadout 的核心路径完全不可用，按 P1 跟踪。

### 建议修法
统一 Console 查询与项目 loadout 校验的数据所有者为 Server `CONN_DEFS`；保留项目中既有的连接器展示名称存储格式，并增加目录、校验分类一致性的回归测试。不要恢复一份平行的 `NP_CONNS` Server 目录。

### 验证
运行库 `CONN_DEFS` 可被项目设置读取；真实页面显示连接器卡片，可选择、保存并通过项目 API 回读，再恢复原值；服务端接受启用的 `CONN_DEFS` 名称、拒绝未知名称，Console 类型检查与生产构建通过。

### 处理记录

- **根因**：Server-first 目录迁移后，连接器定义权威分类已是 `CONN_DEFS`，但项目设置查询和 `_validate_loadout` 仍保留旧展示目录名 `NP_CONNS`。运行库实测 `CONN_DEFS=6`、`NP_CONNS=0`，因此前端恒为空且服务端无法接受真实目录值。
- **修复范围**：Console 项目设置和 Server 项目 loadout 校验统一改读 `CONN_DEFS`，继续以连接器展示名称写入项目字段，不新增或恢复平行的旧目录。
- **防回归**：新增 `server/tests/test_wb515_project_connector_catalog.py`，在隔离数据库中证明启用的 `CONN_DEFS` 名称可以更新项目、未知名称被拒绝，并约束 Console 与 Server 使用同一分类。
- **验证结果**：相关 10 条 Server 测试、`py_compile`、Console 类型检查、生产构建和产物一致性检查通过；硬重启 Server 后，真实项目页面读到 6 张连接器卡片，“本地便签”保存/刷新回读成功，再取消保存并确认恢复原值；修复后新页面无 warning/error。

<!-- issue-record:end WB-515 -->

---

<!-- issue-record:start {"id":"WB-516","number":516,"source":"WB-516-task-editor-collaboration-delivery-tab.md","title":"Console 任务编辑的协作与执行交付区域脱离页签分组","severity":"P2","area":"ui","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-516"></a>

## WB-516 · Console 任务编辑的协作与执行交付区域脱离页签分组

<details>
<summary>原始元数据</summary>

```yaml
id: WB-516
title: Console 任务编辑的协作与执行交付区域脱离页签分组
severity: P2
area: ui
status: fixed
origin: 🆕 近期改动
files:
  - console/src/components/project/ProjectWorkspace.tsx:1014
  - console/src/components/project/ProjectWorkspace.tsx:1520
  - console/src/components/project/ProjectWorkspace.tsx:1597
created: 2026-08-11
```

</details>

### 问题
任务编辑抽屉虽然使用“内容与属性 / 计划与关系 / 更多信息”三页签组织字段，但“任务协作”和“Local Agent 执行与交付”写在页签内容之外。打开默认“内容与属性”时，这两个不同工作阶段的区域会继续排在内容字段下方，页签不能表达或控制它们的归属。

### 触发场景
打开任一已有任务的编辑抽屉并停留在“内容与属性”；向下滚动即可看到任务协作以及 Local Agent 执行与交付，切换其他页签后它们仍然存在。

### 影响
任务内容编辑、团队沟通和执行交付混在同一浏览流中，抽屉过长且信息层级失真；用户难以通过页签直接定位协作记录、Run 状态与交付验收入口，按 P2 跟踪。

### 建议修法
为已有任务新增独立“协作与交付”页签，将任务评论、活动和 Local Agent 执行交付统一放入该页签；新建任务时不显示无法使用的页签。任务字段保存与评论提交保持独立表单边界，不改变现有真实数据和权限逻辑。

### 验证
在真实任务抽屉确认“内容与属性”只显示内容与属性，“协作与交付”集中显示评论、活动和执行交付；新建任务不显示该页签。分别验证任务保存和评论发送，明暗主题及窄宽下无横向溢出，Console 类型检查与生产构建通过。

### 处理记录（2026-08-11）
- 改动：`console/src/components/project/ProjectWorkspace.tsx` 为已有任务新增“4 协作与交付”，集中承载任务评论、活动和 Local Agent 执行交付；子任务只随“计划与关系”显示，新建任务不渲染第四页签。
- 验证：Console 类型检查、生产构建和 dist 一致性检查通过；真实任务在“内容与属性”中不再显示协作/执行区，第四页签两区均可见。760×900 明暗主题下无页面或页签横向溢出，新建任务无第四页签，浏览器 warning/error 为 0。
- commit：随本次修复提交。

<!-- issue-record:end WB-516 -->

---

<!-- issue-record:start {"id":"WB-517","number":517,"source":"WB-517-server-workspace-desktop-companion.md","title":"独立 App 业务面与 Server Workspace 重叠且缺少桌面伴侣收缩路径","severity":"P1","area":"fullstack","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-517"></a>

## WB-517 · 独立 App 业务面与 Server Workspace 重叠且缺少桌面伴侣收缩路径

<details>
<summary>原始元数据</summary>

```yaml
id: WB-517
title: 独立 App 业务面与 Server Workspace 重叠且缺少桌面伴侣收缩路径
severity: P1
area: fullstack
status: fixed
origin: 既有实现
files:
  - console/src/App.tsx:41
  - console/src/pages/OverviewPage.tsx:1
  - console/src/components/project/WorkItemExecution.tsx:239
  - src/App.tsx:37
  - src-tauri/src/lib.rs:1
  - docs/agentmate-server-first-架构设计.md:32
created: 2026-08-11
```

</details>

### 问题

Server-first 已把项目、任务、Session、Run、资产和调度收敛到 Server，但普通用户在 Server 端打开的仍是偏管理视角的 Console 概览；执行等待回答或授权时，Console 又要求回到独立 App。与此同时，App 继续承载项目入口、业务工作台和本机设备能力，导致 Server Workspace 与独立 App 的产品边界重复，也使后续删除独立 App 业务前端没有可验收的迁移路径。

### 触发场景

普通用户登录 Server → 进入首页或项目任务执行详情 → 无法在 Server 端形成“我的行动项、最近执行、等待我处理”的个人闭环；Run 进入 `waiting_user` / `waiting_approval` → 页面仅提示前往 App。若现在直接删除 App，则对话、授权、产物与本机能力入口中断；若继续扩展 App，则重新形成两套业务前端。

### 影响

P1：产品入口长期重复会增加前后端维护和用户选择成本，也阻塞既定的 Server 业务权威与 Desktop Companion/Local Agent 本机信任边界收敛。直接删除则会破坏当前唯一完整的执行闭环。

### 建议修法

先交付可回退的第一阶段纵向切片：Server 提供普通用户可用的个人 Workspace，聚合真实行动项、需人工介入和最近执行，并提供继续处理入口；Console 管理能力仅对管理员呈现。桌面端文案和导航收敛为 Desktop Companion，继续承载本机凭据、Skill/MCP、文件、可信授权、诊断、托盘和更新。不得删除 Local Agent，也不得在 Server 不可达时伪造业务成功。后续再按真实验收逐步移除 App 中已由 Workspace 接管的业务页面和过渡 API。

### 验证

- 普通账号登录 Server 后默认看到个人 Workspace，不出现平台治理卡片；管理员仍能进入完整 Console。
- Workspace 使用真实 Server 数据展示行动项、等待回答/授权和最近执行，并可进入对应项目/Run。
- 桌面端明确定位为 Desktop Companion，保留本机执行、授权、文件、凭据和诊断入口，不破坏现有 Run 闭环。
- `npx tsc --noEmit`、Console 类型检查与 App/Console 生产构建通过。
- 在桌面与 390px 宽度下验证 Server Workspace 和桌面入口的浅色/深色主题，无横向溢出和控制台错误。

### 处理记录（2026-08-11）

- 改动：Server `console/` 根入口改为真实个人 Workspace，聚合跨项目行动项、等待用户介入、最近 Run 和当前账号执行节点；管理员管理总览独立到 `/admin`。原 App 入口收敛为 Desktop Companion，本机执行、授权、文件、凭据、Skill/MCP、诊断、托盘和更新边界保持不变；README、AGENTS 与 Server-first 权威架构同步更新。
- 验证：WB-517 回归 2/2、App/Console `tsc --noEmit`、两套 Vite 生产构建及 `git diff --check` 通过。真实 `:8100` 读到 2 条 Server 行动项并正确区分 Workspace 与 `/admin`；`:8102` 显示 Desktop Companion 与 Local Agent 在线。两端在桌面和 390×844 浅色/深色下无横向溢出，浏览器 error 日志为 0。仓库聚合 `pnpm build` 仍被 WB-517 范围外的 `sse.ts`、`chatStore.ts`、`workbenchStore.ts` 既有 build-mode 类型错误阻断，未夹带修复。
- commit：随本次修复提交。

<!-- issue-record:end WB-517 -->

---

<!-- issue-record:start {"id":"WB-518","number":518,"source":"WB-518-desktop-home-workspace-duplication.md","title":"Desktop Companion 首页仍复制 Server Workspace 业务工作台","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-518"></a>

## WB-518 · Desktop Companion 首页仍复制 Server Workspace 业务工作台

<details>
<summary>原始元数据</summary>

```yaml
id: WB-518
title: Desktop Companion 首页仍复制 Server Workspace 业务工作台
severity: P1
area: frontend
status: fixed
origin: 🏚 迁移遗留
files:
  - src/views/HomeView.tsx:80
  - src/stores/workbenchStore.ts:27
  - src/components/layout/Sidebar.tsx:27
created: 2026-08-11
updated: 2026-08-11
```

</details>

### 问题

WB-517 已把 Server 根入口建设为普通用户 Workspace，但 `src/views/HomeView.tsx` 仍通过 `workbenchStore` 读取并展示 Server 行动项、Run、待回答、待恢复和待验收交付，形成第二套业务首页。Desktop Companion 的首页定位与实际数据流不一致，独立 App 业务面仍无法继续退役。

### 触发场景

登录后分别打开 Server `:8100` 与 Desktop `:8102` → 两端都展示行动项、Run 和需要用户处理的业务状态 → 用户无法判断应该在哪一端推进任务，前端也要维护两套同类聚合逻辑。

### 影响

P1：重复业务入口会持续制造状态心智与维护成本，并阻塞“Workspace 负责业务、Desktop Companion 负责本机信任边界、Local Agent 负责执行”的既定收敛路径。

### 建议修法

将 Desktop 首页收敛为真实执行节点控制面：展示 Local Agent 在线状态、Server 连接、当前本机执行入口与本机能力快捷入口，并提供清晰的 Server Workspace 主入口。移除首页对 `workbenchStore` 行动项、Run 与交付验收聚合的依赖；项目上下文与现有 Run 执行页暂时保留，避免破坏执行闭环。不得伪造设备或业务状态。

### 验证

- Desktop 首页不再展示或请求 Server 行动项、最近 Run、待验收等重复业务聚合。
- 首页仍展示真实 Local Agent/Server 状态，可进入本机能力、任务上下文与 Server Workspace。
- 现有项目上下文和 Run 执行入口保持可达。
- App 类型检查、生产构建和回归测试通过。
- 在桌面与 390px 宽度下检查浅色/深色主题，无横向溢出和控制台错误。

### 处理记录（2026-08-11）

- 改动：`src/views/HomeView.tsx` 移除 `workbenchStore`、行动项、跨项目 Run、待验收和业务缓存聚合，改为真实 Local Agent/Server 探测、租约、WAL、working copy、本机能力和诊断入口；保留 Composer、任务上下文与现有执行闭环。默认启动页文案同步为“执行与授权”，历史产品契约回归迁移到 Workspace/Desktop Companion 新边界。
- 验证：相关回归 21/21、`pnpm exec tsc --noEmit`、App Vite 生产构建和 `git diff --check` 通过。真实 `:8102` 显示 Local Agent/Server 在线、活动租约 0、WAL 0、3 个已绑定身份与 8 个 working copy；桌面及 390×844 浅色/深色下无重复业务聚合、无横向溢出，浏览器 error 日志为 0；任务上下文仍可进入 `/projects`。
- commit：随本次修复提交。

<!-- issue-record:end WB-518 -->

---

<!-- issue-record:start {"id":"WB-519","number":519,"source":"WB-519-server-workspace-run-handoff.md","title":"Server Workspace 缺少原子发起执行与 Desktop Companion 会话交接","severity":"P1","area":"fullstack","status":"fixed","created":"2026-08-11","archive_format":2} -->
<a id="wb-519"></a>

## WB-519 · Server Workspace 缺少原子发起执行与 Desktop Companion 会话交接

<details>
<summary>原始元数据</summary>

```yaml
id: WB-519
title: Server Workspace 缺少原子发起执行与 Desktop Companion 会话交接
severity: P1
area: fullstack
status: fixed
origin: 🆕 近期改动
files:
  - console/src/pages/WorkspacePage.tsx
  - console/src/workspaceApi.ts
  - console/src/desktopHandoff.ts
  - console/src/components/project/WorkItemExecution.tsx
  - console/src/styles.css
  - src/platform/deepLinks.ts
  - src/App.tsx
  - src-tauri/src/lib.rs
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/Cargo.toml
  - package.json
  - backend/tests/regression/test_wb519_server_workspace_handoff.py
created: 2026-08-11
```

</details>

### 问题
WB-517/WB-518 已把 Server 确立为业务权威并将 Desktop 首页收缩为执行节点工作台，但 Server Workspace 当前只能读取行动项、Run 和设备，不能创建新执行；Run 等待本机回答或授权时也只有文字提示，无法稳定定位到 Desktop Companion 的对应会话。用户仍必须从 Desktop 的 Composer 发起业务，阻断了业务面完全回归 Server 的路径。

### 触发场景
用户在 Server Workspace 查看自己的项目、行动项和执行节点后，希望直接发起 ask/plan/exec Run；或 Run 进入 `waiting_user` / `waiting_approval` 后，需要打开领取它的本地 Desktop Companion 继续处理。

### 影响
用户必须在 Server 与 Desktop 之间猜测同一 Run 的会话位置，且 Desktop 仍承担不该保留的业务发起职责。这会导致双主页产品心智反复，也使后续移除 Desktop Composer 时丢失关键能力。

### 建议修法
复用 Server 既有 `POST /api/turns` 原子契约，在 Workspace 增加真实项目、模式和 Local Agent 选择，通过 `Idempotency-Key` 一次创建 Session/User Message/Run；为新建和历史 Run 统一生成 Desktop Companion 会话地址，项目 Run 定位到 `/projects/:projectId/runs/:sessionId`，通用 Run 定位到 `/chat/:sessionId`。不复制 Runtime，不在 Server 执行本机工具。

### 验证
用隔离测试验证 Workspace 调用原子 `/turns` 契约、幂等头、模式/项目/节点载荷与两种会话路由；Console 类型检查和生产构建通过；真实浏览器在桌面、窄屏及明暗主题下可读取真实项目/设备、看到发起入口与正确的执行节点交接链接，不触发真实 LLM 消耗。

### 处理记录

- 2026-08-12：已认领并开始处理；首先复核 Server `POST /api/turns` 与 Desktop 现有路由，确认可在不新建执行协议的前提下完成交接。
- 2026-08-12：Server Workspace 已可选 ask/plan/exec、可写项目与已验证兼容节点，并通过带 `Idempotency-Key` 的 `POST /api/turns` 原子创建 Session/User Message/Run；没有将 Runtime 或本机工具移入 Server。
- 2026-08-12：增加 `agentmate://open/run` 安装包深链，Desktop 只接受指定 scheme/host/path 和受限 ID，并转换为现有 `/chat/:sessionId` 或 `/projects/:projectId/runs/:sessionId` 路由；Tauri 官方 deep-link 与 single-instance 插件保证已运行实例被唤起而不重复启动窗口。
- 2026-08-12：WB-519 回归 3/3、Server 原子 Turn 用例、Console `tsc`/生产构建、Desktop Vite 生产构建、Tauri `cargo check` 与 2/2 Rust 测试全部通过。真实 `:8100` 读到账号 `chenqi`、项目 `koda`、行动项与当前无注册节点状态；桌面与 390×844 明暗双主题无横向溢出，浏览器 error 为 0，仅验证表单门禁而未提交真实 Run。Desktop 全量 `tsc` 仍只被 WB-519 范围外的 `sse.ts`/`chatStore.ts`/`workbenchStore.ts` 既有类型错误阻断。

<!-- issue-record:end WB-519 -->

---

<!-- issue-record:start {"id":"WB-520","number":520,"source":"WB-520-desktop-business-run-launch-retirement.md","title":"Desktop Companion 仍可新建 Run 并复制 Server 任务上下文","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-520"></a>

## WB-520 · Desktop Companion 仍可新建 Run 并复制 Server 任务上下文

<details>
<summary>原始元数据</summary>

```yaml
id: WB-520
title: Desktop Companion 仍可新建 Run 并复制 Server 任务上下文
severity: P1
area: frontend
status: fixed
origin: 🏚 迁移遗留
files:
  - src/views/HomeView.tsx:1
  - src/components/layout/Sidebar.tsx:1
  - src/views/WorkspaceContextsView.tsx:1
  - src/views/ChatView.tsx:1
  - src/views/ProjExecView.tsx:1
  - src/App.tsx:1
  - src/lib/router.ts:1
created: 2026-08-12
```

</details>

### 问题

WB-517～WB-519 已把 Server Workspace 确立为业务权威和 Run 发起面、把 Desktop Companion 收敛为本机可信控制面，但 Desktop 首页、侧栏、任务上下文页以及已结束执行页仍可直接创建或续写 Run，形成双发起面和双业务语义。

### 触发场景

用户可在 Desktop 首页 Composer、侧栏“新建任务”、`/projects` 任务上下文、空白 `/chat/new` 或已结束 Run 的 Composer 中输入新意图并直接调用 Local Agent；项目执行页还承载 Server 任务列表与计划流转等业务操作。

### 影响

P1：同一业务意图可能绕过 Server 的项目、成员、调度和审计权威直接在设备侧生成 Run，导致跨设备状态、权限和验收入口再次分叉，也模糊 Desktop Companion 与 Local Agent 的最终产品边界。

### 建议修法

- 退役 Desktop 首页 Composer、侧栏“新建任务”和本地任务上下文页；新建意图统一交接到 Server Workspace。
- 禁止 `/chat/new`、`/projects/:id/runs/new` 等本地新建深链；只允许打开 Server 已存在的 session/Run。
- 已存在执行只保留消息与事件观察、Ask User、暂停/恢复/取消、重试、工件和本机诊断；结束后续跑回到 Server Workspace 发起。
- 从项目执行页移除 Server 任务列表、任务详情和计划流转等业务写操作，保留执行节点职责。

### 验证

Desktop 类型检查与生产构建通过；静态契约证明所有本地新建入口和 `new` 深链已移除。真实浏览器在桌面/窄屏、明暗主题验证首页和既有 Run：可以查看本机状态、能力、执行事件与授权控制，但不能创建新业务 Run；Server Workspace 交接入口可用且控制台无新增错误。

### 处理记录

#### 2026-08-12（进行中）

- 已确认 Server Workspace 与 Local Agent 的职责边界，开始逐页退役 Desktop Companion 的业务 Run 发起和任务上下文写入口。

#### 2026-08-12（已修复）

- Desktop 首页 Composer、快捷提示、任务上下文选择器和侧栏“新建任务”已删除；本机能力区只剩技能、连接器、文件、凭据与运行设置，最近执行仍可打开 Server 已存在的 session。
- `/chat/new` 与 `/projects/:id/runs/new` 不再是有效路由，缺少真实 session ID 时统一回到首页或 Server 项目交接页；`/projects` 与灵感入口改为 Server Workspace 职责说明。
- 普通会话和项目 Run 在运行中继续保留 Ask User、暂停、恢复、取消与重试，结束后显示 Server Workspace 交接，不再出现可续写的新意图 Composer。
- 项目执行页删除 Server 任务列表、任务详情和计划流转写操作，只保留消息/事件、只读协作、执行控制、导出和本机工件面板；技能/连接器“去试试”和 Agent 创建技能也不再生成本地草稿。
- 验证：WB-520 定向回归 4/4、产品品牌与技能市场边界回归 8/8、`npx tsc --noEmit`、`npx vite build`、`git diff --check` 均通过。真实 `:8102` 浏览器验证 `/chat/new` 回到 `/`、`/projects` 只做 Server 交接；1280px 与 390×844、明暗主题均无水平溢出，首页 `.composer` 与“新建任务”均为 0，控制台无 error。未创建真实 Run。
- 附加 Ant Design 迁移套件有 2 条既有失败，分别指向 `Sidebar.tsx` 的原生范围按钮和既有无边框 CSS 选择器；相关代码不属于本 issue 改动，未夹带处理。

<!-- issue-record:end WB-520 -->

---

<!-- issue-record:start {"id":"WB-521","number":521,"source":"WB-521-desktop-file-knowledge-entry-retirement.md","title":"Desktop Companion 仍保留文件与知识聚合业务入口","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-521"></a>

## WB-521 · Desktop Companion 仍保留文件与知识聚合业务入口

<details>
<summary>原始元数据</summary>

```yaml
id: WB-521
title: Desktop Companion 仍保留文件与知识聚合业务入口
severity: P1
area: frontend
status: fixed
origin: 🏚 迁移遗留
files:
  - src/components/layout/Sidebar.tsx:1
  - src/views/HomeView.tsx:1
  - src/App.tsx:1
  - src/lib/router.ts:1
  - src/lib/types.ts:1
  - src/views/ConsoleHandoffView.tsx:1
  - src/components/connector/ConnectorDetailModal.tsx:1
  - src/views/ExpertsView.tsx:1
  - src/views/KnowledgeView.tsx:1
created: 2026-08-12
```

</details>

### 问题
WB-517～WB-520 已把业务意图、项目、Run 发起与验收收回 Server Workspace，并把 Desktop Companion 收敛为本机可信控制面；但侧栏仍以“文件与知识”聚合“我的文件、金山文档、知识库、灵感”，首页仍提供“本机文件”快捷入口，形成第二套业务信息架构。

### 触发场景
用户在 Desktop Companion 点击“文件与知识”或首页“本机文件”，会进入混合 Server 交付物、本机工作区、外部 SaaS 文档、本机 RAG 知识源和 Server 灵感的独立页面；旧 `/files`、`/kdocs`、`/inspiration` 路由也继续暴露这些页面。

### 影响
P1：文件、知识、连接器和业务发现的权威归属再次混杂。用户无法判断交付物应在 Run 内验收、KDocs/WeKnora 应作为执行能力配置，还是应在 Desktop 独立管理；这会延续双产品心智并阻碍独立 App 业务面的彻底退役。

### 建议修法
- 删除整个“文件与知识”聚合菜单、底部“灵感”和首页“本机文件”入口。
- 退役“我的文件、金山文档、灵感”独立前端页面与静态路由；旧 URL 回到 Desktop 执行节点首页。
- 文件仅在具体 Run 的工件/工作区面板出现；KDocs 仅作为连接器配置与运行时能力出现。
- WeKnora 保留连接器内部的本机知识源管理入口，但移除“挂载到对话”等本地业务发起语义；底层文件、知识库、连接器 API 与执行能力不删除。

### 验证
定向契约测试、Desktop 类型检查和生产构建通过；真实浏览器验证桌面与 390×844、明暗主题下不再出现“文件与知识、我的文件、金山文档、灵感、本机文件”业务入口，无水平溢出和控制台错误；旧 `/files`、`/kdocs`、`/inspiration` 回到首页，连接器内部仍可进入本机知识源管理，且不触发真实 Run。

### 处理记录

- 2026-08-12：已认领并开始处理；按更激进边界退役聚合菜单和独立业务页，同时保留 Run 工件与连接器内部的本机执行能力。
- 2026-08-12：已删除侧栏“文件与知识”、底部“灵感”、首页“本机文件”和旧静态路由；`inspire/myfiles/kdocs` 也退出 `ViewId`，对应旧页面不再被 App 引用或进入生产构建。旧 `/files`、`/kdocs`、`/inspiration` 统一回到执行节点首页。
- 2026-08-12：连接器卡片不再提供本地会话加号或“添加到本会话”；KDocs 只保留 OAuth/运行时连接能力，文件只保留在具体 Run 的工件面板。WeKnora 作为连接器次级操作提供“管理本机知识源”，页面只管理设备侧建库、文档和向量化，不再挂载本地对话。
- 2026-08-12：WB-521/WB-520 回归 8/8、`npx tsc --noEmit`、`npx vite build`、`git diff --check` 均通过。真实 `:8102` 验证旧 URL 回退、连接器→本机知识源可达；1280px 与 390×844、明暗主题均无水平溢出，浏览器日志 0 error / 0 warning，未创建真实 Run。

<!-- issue-record:end WB-521 -->

---

<!-- issue-record:start {"id":"WB-522","number":522,"source":"WB-522-packaged-sidecar-core-mode.md","title":"正式桌面 sidecar 未进入最小 Local Agent Core 模式","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-522"></a>

## WB-522 · 正式桌面 sidecar 未进入最小 Local Agent Core 模式

<details>
<summary>原始元数据</summary>

```yaml
id: WB-522
title: 正式桌面 sidecar 未进入最小 Local Agent Core 模式
severity: P1
area: backend
status: fixed
origin: 🏚 迁移遗留
files:
  - src-tauri/src/lib.rs:370
  - backend/main.py:282
  - scripts/smoke-local-agent-sidecar.ps1:49
created: 2026-08-12
```

</details>

### 问题

正式 Tauri 启动 sidecar 时只传 `--ipc-token-stdin`，而 `backend/main.py` 仅在收到 `--local-agent-core` 时选择最小 Core app；因此安装版实际启动包含全部兼容业务路由的完整后端。冒烟脚本同时传入两个参数，验证对象与正式启动对象不一致。

### 触发场景

启动正式 Desktop Companion → Tauri 拉起 `agentmate-local-agent --ipc-token-stdin` → `core_mode` 为 false → 完整 `app` 启动。

### 影响

P1：破坏 Server 管理、Local Agent 执行的目标边界，保留不必要的本地业务权威和攻击面，且现有冒烟测试会给出错误信心。

### 建议修法

正式 sidecar 与冒烟脚本统一传入 Core 模式；增加 Rust/契约测试确认启动参数与最小路由集合，避免回退到兼容 app。

### 验证

- Rust 测试与 sidecar smoke 通过。
- 正式启动命令包含 `--local-agent-core --ipc-token-stdin`。
- Core OpenAPI/HTTP 仅暴露 `/api/local-agent/*`，旧业务 API 返回 404。

### 处理记录（2026-08-12）

- 改动：正式 Tauri sidecar 统一使用 `--local-agent-core --ipc-token-stdin`，抽取固定参数函数并增加 Rust 契约测试；同步更新 WB-434 Core 回归，确保测试对象与安装版一致。
- 验证：`cargo test --manifest-path src-tauri/Cargo.toml` 3/3 通过；WB-434/435/436 定向回归 13/13 通过，最小 Core 路由与 IPC/working copy 边界保持通过。

<!-- issue-record:end WB-522 -->

---

<!-- issue-record:start {"id":"WB-523","number":523,"source":"WB-523-app-release-build-contract-errors.md","title":"App 完整发布构建被计划事件与工作台状态类型错误阻断","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-523"></a>

## WB-523 · App 完整发布构建被计划事件与工作台状态类型错误阻断

<details>
<summary>原始元数据</summary>

```yaml
id: WB-523
title: App 完整发布构建被计划事件与工作台状态类型错误阻断
severity: P1
area: frontend
status: fixed
origin: 既有实现
files:
  - src/lib/types.ts:91
  - src/lib/sse.ts:81
  - src/stores/chatStore.ts:58
  - src/stores/workbenchStore.ts:16
created: 2026-08-12
```

</details>

### 问题

`RunPlanEvent.version` 在前端被声明为可选，但 Trace 与后端 SSE 契约要求必填；`workbenchStore` 又依赖回调副作用传出解析状态，TypeScript build-mode 无法证明该值已赋值。`pnpm build` 因此失败。

### 触发场景

仓库根运行 `pnpm build` → `tsc -b` 在 `sse.ts`、`chatStore.ts`、`workbenchStore.ts` 报错 → Tauri `beforeBuildCommand` 无法完成。

### 影响

P1：正式安装包不能从当前源码构建；单独 Vite build 的成功不能替代类型契约门禁。

### 建议修法

让计划事件类型与真实 SSE 协议一致，并把 Server 读取结果设计成显式返回解析状态，避免依赖回调闭包副作用。

### 验证

- `pnpm build` 通过。
- 计划 snapshot/patch 回归覆盖 version。
- live/cache 两种 Server read 来源均被单元测试覆盖。

### 处理记录（2026-08-12）

- 改动：`RunPlanEvent.version` 与已校验的 Server SSE/Trace 契约统一为必填；工作台 Server read 改用显式结果盒并在缺失解析状态时失败，消除 build-mode 的闭包副作用误判；新增契约回归。
- 验证：聚合 `pnpm build` 完整通过 App `tsc -b`、App Vite、Console TypeScript 与 Console Vite；WB-523 回归 2/2 通过。

<!-- issue-record:end WB-523 -->

---

<!-- issue-record:start {"id":"WB-524","number":524,"source":"WB-524-release-gate-drift-and-main-ci.md","title":"发布回归契约漂移且 main 分支推送不触发 CI","severity":"P1","area":"misc","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-524"></a>

## WB-524 · 发布回归契约漂移且 main 分支推送不触发 CI

<details>
<summary>原始元数据</summary>

```yaml
id: WB-524
title: 发布回归契约漂移且 main 分支推送不触发 CI
severity: P1
area: misc
status: fixed
origin: 🏚 迁移遗留
files:
  - .gitea/workflows/quality.yml:6
  - scripts/validate-v1-rc.ps1:41
  - backend/tests/integration/test_wb112_server_backend_http.py:35
  - backend/tests/regression/test_wb517_server_workspace_companion.py:55
created: 2026-08-12
```

</details>

### 问题

质量工作流只监听 `master`，仓库实际开发/远端分支为 `main`。回归集仍引用已删除的 Desktop 业务 View，集成测试仍通过已经明确废止的环境变量配置 Server 地址；完整门禁因而无法作为当前架构的发布证据。

### 触发场景

- 直接推送 `main`：push workflow 不运行。
- 跑 Local Agent 回归：出现 18/19 failures 与 2 errors。
- 跑集成测试：4 项中 3 项因 Server 未配置失败。

### 影响

P1：坏提交可能绕过 CI；本地发布脚本持续红灯，真实回退和过期断言混在一起。

### 建议修法

工作流对齐 `main`；按 WB-517～521 后的新可达边界更新或删除陈旧回归；集成测试通过真实设备设置数据库配置 Server；保持未跟踪测试门禁。

### 验证

- 完整 `validate-v1-rc.ps1` Engineering lane 通过。
- `main` push 触发 workflow。
- 回归与集成测试连续运行结果稳定。

### 处理记录（2026-08-12）

- 根因：Gitea push workflow 仍监听旧 `master`；发布集成继续启动已退役的本地业务镜像或通过废止环境变量注入 Server；大量静态回归仍以已删除的 Desktop 业务面为权威；账号鉴权单测还暗中依赖其他测试先初始化模型设置表。
- 改动：CI push 分支改为 `main`；集成测试改用 Server 业务权威和设备设置数据库；删除/重写旧 Desktop 业务契约；账号鉴权测试显式隔离不属于鉴权断言的模型配置读取，确保干净、独立顺序也可运行。未跟踪测试门禁保持严格不变。
- 验证：在仅含提交内容的隔离 worktree 运行 `validate-v1-rc.ps1`，10 个 Engineering 门禁全部通过：归档一致性、App/Console 类型与生产构建、Local Agent 462 项、Server 185 项、HTTP 集成 3 项、350 个跟踪 Python 源编译。当前共享工作区会按设计阻断 WB-437 与 WB-494/WB-495 的两份未跟踪测试；`.gitea/workflows/quality.yml` 已静态确认监听 `main`，真实远端 push 触发留给后续实际推送验证。

<!-- issue-record:end WB-524 -->

---

<!-- issue-record:start {"id":"WB-525","number":525,"source":"WB-525-sensitive-db-backup-ignore-gap.md","title":"真实 SQLite 备份未被仓库忽略规则覆盖","severity":"P1","area":"misc","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-525"></a>

## WB-525 · 真实 SQLite 备份未被仓库忽略规则覆盖

<details>
<summary>原始元数据</summary>

```yaml
id: WB-525
title: 真实 SQLite 备份未被仓库忽略规则覆盖
severity: P1
area: misc
status: fixed
origin: 既有实现
files:
  - .gitignore:18
  - backend/agentmate.db.bak-20260805124440:1
created: 2026-08-12
```

</details>

### 问题

仓库内存在未跟踪的 `backend/agentmate.db.bak-20260805124440`。只读结构检查确认它含真实消息以及 password hash、auth token、Server token、API key、bot token、Webhook secret 等字段；当前 `.gitignore` 只覆盖固定 live DB 文件名。

### 触发场景

在仓库内生成带时间戳的 SQLite 备份 → `git status` 显示 `??` → 误用宽范围暂存时可进入提交。

### 影响

P1：尚未确认已泄露，但属于可导致凭据、会话和用户内容进入 Git 历史的高风险路径。

### 建议修法

增加精确的 AgentMate 数据库备份/日志忽略规则，并把现有敏感备份移到既有仓库外 ACL 备份目录；不删除唯一备份。

### 验证

- 备份在仓库外仍存在且哈希一致。
- `git check-ignore` 命中备份命名模式。
- `git status` 不再列出数据库备份与运行日志。

### 处理记录（2026-08-12）

- 改动：将 SQLite 备份及其 `-shm`/`-wal` 三个文件迁移到仓库外 `C:\Users\chenq\AgentMate-Backups\repository-quarantine-20260812`；增加 AgentMate 带时间戳 DB 备份、Server 本地 DB 和根运行日志的精确忽略规则。
- 验证：三文件迁移前后 SHA-256 一致（主文件 `3D793E6BC3ED9457AC43319D3B717FDF75BF953D3684CD0F7C9453A9F5A0C9AC`）；`git check-ignore -v` 对备份、Server DB 与三类运行日志全部命中，`git status` 不再显示它们。

<!-- issue-record:end WB-525 -->

---

<!-- issue-record:start {"id":"WB-526","number":526,"source":"WB-526-work-item-planning-capability-registry.md","title":"WorkItem 计划工具实现未进入设备能力注册表","severity":"P2","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-526"></a>

## WB-526 · WorkItem 计划工具实现未进入设备能力注册表

<details>
<summary>原始元数据</summary>

```yaml
id: WB-526
title: WorkItem 计划工具实现未进入设备能力注册表
severity: P2
area: backend
status: fixed
origin: 🆕 近期改动
files:
  - backend/agent/tools.py:972
  - backend/agent/skills.py:367
  - server/catalog_seed.py:36
created: 2026-08-12
```

</details>

### 问题

`update_work_item_planning` 已有真实 Tool 实现且 Server 目录已发布，但 `_TOOL_REGISTRY` 未导入/注册它。设备能力报告由该注册表生成，导致目录兼容判断与真实实现不一致。

### 触发场景

Local Agent 上报 capability report → `supported_tools` 缺少该工具 → Server 对依赖它的能力作 unsupported 判断。

### 影响

P2：可造成错误的不兼容状态，并使工具目录契约回归失败。

### 建议修法

把真实 Tool 纳入统一注册表并保持 exposure/bindable 语义不变，不把 contextual 写工具错误开放给普通 Skill。

### 验证

- `test_runtime_tool_registry_and_capability_report_come_from_real_implementations` 通过。
- exec/plan 模式暴露边界不变。

### 处理记录（2026-08-12）

- 改动：将 `update_work_item_planning` 的真实 Tool 对象加入统一 `_TOOL_REGISTRY`；保留 contextual、不可绑定普通 Skill 的既有目录策略。
- 验证：工具目录/能力报告契约与 WB-504 计划工具回归 4/4 通过，exec/plan 暴露边界保持通过。

<!-- issue-record:end WB-526 -->

---

<!-- issue-record:start {"id":"WB-528","number":528,"source":"WB-528-tauri-content-security-policy.md","title":"Desktop WebView 未配置 Content Security Policy","severity":"P2","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-528"></a>

## WB-528 · Desktop WebView 未配置 Content Security Policy

<details>
<summary>原始元数据</summary>

```yaml
id: WB-528
title: Desktop WebView 未配置 Content Security Policy
severity: P2
area: frontend
status: fixed
origin: 既有实现
files:
  - src-tauri/tauri.conf.json:27
  - src/lib/api.ts:13
created: 2026-08-12
```

</details>

### 问题

Tauri 配置将 `csp` 设为 null，而 Desktop WebView 持有 Server Bearer token 并能调用窄化的原生命令。Markdown 已做 DOMPurify，但缺少 WebView 级纵深防护。

### 触发场景

任一未来 XSS/依赖注入进入 WebView → CSP 不会阻断非预期脚本、连接或资源加载。

### 影响

P2：当前未发现直接 XSS，但一旦前端注入发生，身份 token 与本机命令桥的影响面更大。

### 建议修法

按实际静态资源、Server HTTPS、Local Agent loopback 与必要图片协议配置最小 CSP；不得用宽泛 `*` 或 `unsafe-eval` 规避问题。

### 验证

- Tauri build/cargo test 通过。
- App、Server 登录、Local Agent 状态、Markdown、更新检查与深链正常。
- 非白名单脚本/连接被 CSP 拒绝。

### 处理记录（2026-08-12）

- 改动：为 Tauri WebView 配置显式 CSP，仅允许自身脚本、Tauri IPC/asset、本机 loopback Local Agent、HTTPS/WSS Server 以及必要的 data/blob 图片和内联样式；禁止 object、外部脚本、`unsafe-eval`、非自身 base 与 frame 嵌入。
- 验证：CSP 契约回归通过；`cargo test` 3/3、App+Console `pnpm build` 通过。真实浏览器中 Local Agent/Server 状态、设置与主题页面正常；Tauri 原生 WebView 的拒绝日志仍应在正式安装包 smoke 中持续观察。

<!-- issue-record:end WB-528 -->

---

<!-- issue-record:start {"id":"WB-529","number":529,"source":"WB-529-desktop-placeholder-settings-retirement.md","title":"Desktop 设置仍展示不可用的快捷键与支持入口","severity":"P2","area":"ui","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-529"></a>

## WB-529 · Desktop 设置仍展示不可用的快捷键与支持入口

<details>
<summary>原始元数据</summary>

```yaml
id: WB-529
title: Desktop 设置仍展示不可用的快捷键与支持入口
severity: P2
area: ui
status: fixed
origin: 🏚 迁移遗留
files:
  - src/components/settings/SettingsModal.tsx:844
created: 2026-08-12
```

</details>

### 问题

Desktop 设置仍展示“快捷键”“提交反馈”“帮助文档”，但前者只是“即将上线”面板，后两者只弹 Toast。它们不是本机执行闭环的必要能力，也没有真实后端或文档目标。

### 触发场景

用户打开设置并点击相关入口 → 得不到任何可完成动作。

### 影响

P2：形成虚假可用入口，削弱 Desktop Companion 收缩后的信息架构。

### 建议修法

更激进地移除没有真实实现的入口及只服务于它们的占位组件；未来有真实目标时再以独立 issue 恢复。

### 验证

- 设置导航只保留真实可用页面。
- 明暗主题和窄宽设置导航正常。
- 类型检查与设置回归通过。

### 处理记录（2026-08-12）

- 改动：从设置类型、桌面/移动导航和内容区移除“快捷键”“帮助与反馈”及其 Toast/`Soon` 占位实现；同时移除数据管理、安全中心中没有真实执行链路的未来能力提示，只保留可完成的真实设置页。
- 验证：设置退役契约与全量 469 条回归通过，`pnpm build` 通过；真实浏览器在浅色、深色和 640×800 窄宽下验证导航仅含真实页面，移动下拉显示、桌面菜单隐藏、无横向溢出，暗色背景/文字可读。

<!-- issue-record:end WB-529 -->

---

<!-- issue-record:start {"id":"WB-530","number":530,"source":"WB-530-packaged-core-retained-device-apis.md","title":"安装版 Core 启动后保留的设备管理 API 全部失效","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-530"></a>

## WB-530 · 安装版 Core 启动后保留的设备管理 API 全部失效

<details>
<summary>原始元数据</summary>

```yaml
id: WB-530
title: 安装版 Core 启动后保留的设备管理 API 全部失效
severity: P1
area: backend
status: fixed
origin: 🆕 近期改动
files:
  - src-tauri/src/lib.rs:32
  - backend/local_agent_core.py:337
  - src/lib/api.ts:35
created: 2026-08-12
```

</details>

### 问题

WB-522 将正式 Tauri sidecar 改为 `--local-agent-core`，但 `local_agent_core.app` 只注册
`/api/local-agent/*`。Desktop Companion 仍保留模型、技能、连接器、运行设置、执行诊断、
记忆与安全等本机设备管理页面，这些页面继续通过 `LOCAL_API_BASE` 调用完整后端路由，
安装版因而稳定得到 404。这是 WB-522 的兼容性回归。

### 触发场景

启动正式 sidecar Core → 打开设置中心或本机能力 → 请求 `/api/settings/runtime`、
`/api/device-diagnostics`、`/api/models`、`/api/skills`、`/api/connectors/local` → 全部 404；
只有 `/api/local-agent/status` 返回 200。

### 影响

安装版 Desktop Companion 无法管理执行节点所需的模型凭据、技能、连接器和运行设置，
诊断入口也不可用；用户无法把这台设备配置成可工作的执行节点。

### 建议修法

在设备 API 完成受控 Core/IPC 迁移前，正式 sidecar 继续启动兼容 app；兼容 app 仍包含受
IPC 保护的 `/api/local-agent/*`，不会丢失 Run/working-copy Core 能力。增加真实路由集合
回归，明确保留设备页面所依赖的 API 必须可达；后续另行完成最小 Core 迁移，不用一次
启动参数切换冒充迁移完成。

### 验证

- 正式 sidecar 启动参数不再选择仅含 `/api/local-agent/*` 的 app。
- Core IPC 状态、身份、Run input 与 working copy 契约继续通过。
- 兼容启动目标下，运行设置、诊断、模型、技能、连接器路由不返回 404。
- `cargo test`、前端类型检查与相关 Python 回归通过。

### 处理记录（2026-08-12）

- 改动：正式 Tauri sidecar 恢复以 `--ipc-token-stdin` 启动兼容 app；完整 app 继续挂载受 IPC 保护的 Local Agent Core 路由，同时保留设置、诊断、模型、技能与连接器设备 API。新增安装目标路由集合回归，并同步修正 WB-434 启动契约。
- 验证：WB-434/WB-530/WB-528/WB-531 定向回归 10/10；`cargo test` 3/3；`npx tsc --noEmit`、`npx vite build` 与新增测试 `py_compile` 通过。
- commit：本提交

<!-- issue-record:end WB-530 -->

---

<!-- issue-record:start {"id":"WB-531","number":531,"source":"WB-531-tauri-csp-nonloopback-http-server.md","title":"Tauri CSP 静默阻断非 loopback HTTP Server","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-531"></a>

## WB-531 · Tauri CSP 静默阻断非 loopback HTTP Server

<details>
<summary>原始元数据</summary>

```yaml
id: WB-531
title: Tauri CSP 静默阻断非 loopback HTTP Server
severity: P1
area: frontend
status: fixed
origin: 🆕 近期改动
files:
  - src-tauri/tauri.conf.json:27
  - src/lib/channels.ts:62
created: 2026-08-12
```

</details>

### 问题

WB-528 的 `connect-src` 只允许 loopback HTTP 与任意 HTTPS，但设备设置和通道解析仍接受
任意 `http(s)` Server 地址。已有受控局域网验收使用 `http://192.168.20.81:8100`；在
Tauri WebView 中该地址会被 CSP 在请求发出前拦截，而应用只呈现普通连接失败。

### 触发场景

把 Server 地址设为非 loopback 的 `http://<LAN-IP>:8100` → 启动安装版 → 登录、刷新
Workbench 或读取 Server 数据 → WebView 因 `connect-src` 拒绝连接，尽管 Server 本身健康。

### 影响

既有局域网部署升级安装版后无法登录或使用 Server-first 业务通道，而且错误表现与 Server
宕机相同，增加排障成本。

### 建议修法

明确安全边界：Desktop 只允许 loopback HTTP；非 loopback Server 必须使用 HTTPS。
在通道 URL 解析阶段主动拒绝不安全地址并给出可操作错误，不依赖 CSP 静默拦截；设置页
元数据和回归测试同步该约束。保留 CSP 的最小连接面，不放宽成任意 `http:`。

### 验证

- Desktop 接受 `http://127.0.0.1:*`、`http://localhost:*` 与 HTTPS Server。
- Desktop 对局域网/公网 HTTP Server 返回明确的安全错误，且不会发出 fetch。
- Web 浏览器开发代理行为不受影响。
- CSP 与通道 URL 策略回归、前端类型检查和生产构建通过。

### 处理记录（2026-08-12）

- 改动：在 Desktop 的统一 Server URL 解析入口增加 CSP 对齐校验；保留 localhost/127.0.0.1 HTTP 与全部 HTTPS，非 loopback HTTP 在任何 fetch 或 Console 跳转前返回明确安全错误。CSP 继续保持最小连接面，没有放宽为任意 `http:`。
- 验证：WB-531/WB-528 与关联 Core 回归 10/10；`npx tsc --noEmit`、`npx vite build`、`cargo test` 3/3 与新增测试 `py_compile` 通过。
- commit：本提交

<!-- issue-record:end WB-531 -->

---

<!-- issue-record:start {"id":"WB-532","number":532,"source":"WB-532-console-handoff-regression-contract.md","title":"Console 交接回归门禁绑定旧实现细节并阻断全量测试","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-532"></a>

## WB-532 · Console 交接回归门禁绑定旧实现细节并阻断全量测试

<details>
<summary>原始元数据</summary>

```yaml
id: WB-532
title: Console 交接回归门禁绑定旧实现细节并阻断全量测试
severity: P1
area: frontend
status: fixed
origin: 🆕 近期改动
files:
  - backend/tests/regression/test_wb460_console_handoff_contract.py:20
  - src/lib/channels.ts:61
created: 2026-08-12
```

</details>

### 问题

WB-531 把 Server URL 校验抽取为 `validateServerRoot` 后，WB-460 回归仍断言 `serverConsoleBase` 函数体内存在 `parsed = new URL(root)`。真实行为没有回退，但全量回归稳定失败。

### 触发场景

运行 Desktop/Local Agent 全量 regression suite。

### 影响

P1：发布门禁持续红灯，真实回归与测试漂移无法区分。

### 建议修法

让回归验证公开行为和稳定调用边界，不再绑定 helper 内部语句位置；增加非法协议、非 URL 与尾部 `/api` 的行为断言。

### 验证

- WB-460/WB-531 定向回归通过。
- Desktop/Local Agent 全量 regression suite 不再因该断言失败。

### 处理记录

2026-08-12：WB-460 回归改为验证 `serverConsoleBase` 调用稳定的 `validateServerRoot` 边界，不再绑定 helper 内部语句位置；新增 TypeScript 行为测试覆盖 URL 规范化与非法 URL/协议拒绝。定向 5 项、前端行为 6 项及 Local Agent 全量回归 476 项通过。

<!-- issue-record:end WB-532 -->

---

<!-- issue-record:start {"id":"WB-533","number":533,"source":"WB-533-open-session-error-swallowed.md","title":"打开执行记录失败被静默吞掉导致路由误判成功","severity":"P2","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-533"></a>

## WB-533 · 打开执行记录失败被静默吞掉导致路由误判成功

<details>
<summary>原始元数据</summary>

```yaml
id: WB-533
title: 打开执行记录失败被静默吞掉导致路由误判成功
severity: P2
area: frontend
status: fixed
origin: 🏚 既有缺陷
files:
  - src/stores/chatStore.ts:233
  - src/App.tsx:140
created: 2026-08-12
```

</details>

### 问题

`openSession` 捕获 `api.getMessages` 的异常后直接忽略并正常 resolve。App 启动恢复因而继续切到目标 route，页面却保留旧会话或空状态，外层 fallback 永远收不到失败。

### 触发场景

通过带 session id 的 URL 启动 App，同时 Server 不可达、会话不存在或无权限。

### 影响

P2：用户看到错误页面状态且没有解释，深链恢复与后退导航行为不可靠。

### 建议修法

在保证当前安全状态不被半更新的前提下把异常重新抛给调用者，由 App 现有恢复边界执行明确 fallback；用行为测试覆盖 reject 与成功路径。

### 验证

- `openSession` 读取失败时 promise reject，且不会写入目标 session 状态。
- App 路由恢复进入既定 fallback；正常会话仍可打开。

### 处理记录

2026-08-12：`openSession` 在 API 失败时保留当前会话并重新抛错，让 App 现有路由恢复边界执行 fallback；成功响应仍原子提交目标会话。真实执行 Zustand action 的成功/失败测试通过，并纳入 TypeScript 与统一 regression 门禁。

<!-- issue-record:end WB-533 -->

---

<!-- issue-record:start {"id":"WB-534","number":534,"source":"WB-534-server-console-security-headers.md","title":"Server Console 缺少浏览器安全响应头基线","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-534"></a>

## WB-534 · Server Console 缺少浏览器安全响应头基线

<details>
<summary>原始元数据</summary>

```yaml
id: WB-534
title: Server Console 缺少浏览器安全响应头基线
severity: P1
area: backend
status: fixed
origin: 🏚 既有缺陷
files:
  - server/main.py:84
created: 2026-08-12
```

</details>

### 问题

Server 同源托管 Console 和业务 API，但响应未设置 CSP、禁止 MIME sniff、点击劫持、referrer、权限策略及 HTTPS HSTS 基线。

### 触发场景

浏览器访问 `/`、Console 静态资源或 `/api/*`，检查响应头。

### 影响

P1：一旦前端依赖或内容注入出现缺陷，浏览器缺少第二道约束；公开 HTTPS 部署也没有强制传输策略。

### 建议修法

增加统一安全响应头 middleware；CSP 只开放当前 Console 所需来源，HSTS 仅在真实 HTTPS/可信转发协议下启用，并用真实 ASGI 请求验证 HTML、静态资源和 API。

### 验证

- Console/API 响应具备预期安全头。
- HTTP 本地开发不发送 HSTS，HTTPS 部署发送 HSTS。
- Console 生产构建仍能加载。

### 处理记录

2026-08-12：新增纯 ASGI `SecurityHeadersMiddleware`，对 Console、静态资源和 API 统一设置 CSP、nosniff、DENY、no-referrer 与 Permissions-Policy；只在请求真实为 HTTPS 时发送一年 HSTS。本地真实 HTTP 读回全部基线且无误发 HSTS，HTTPS/HTTP 单元测试 2/2、Server 全量 192 项通过。

<!-- issue-record:end WB-534 -->

---

<!-- issue-record:start {"id":"WB-535","number":535,"source":"WB-535-mobile-home-touch-targets.md","title":"Desktop Home 窄屏次要操作触控区域不足","severity":"P2","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-535"></a>

## WB-535 · Desktop Home 窄屏次要操作触控区域不足

<details>
<summary>原始元数据</summary>

```yaml
id: WB-535
title: Desktop Home 窄屏次要操作触控区域不足
severity: P2
area: frontend
status: fixed
origin: 🆕 近期改动
files:
  - src/styles/app.css:2265
created: 2026-08-12
```

</details>

### 问题

Desktop Home 在 390px 窄屏下，登录、刷新、重新检查、运行设置及折叠侧栏等操作仍有 28～34px 的点击区域，低于项目既定的 44px 移动触控基线。

### 触发场景

在宽度不超过 640px 的触控设备打开 Home，操作离线提示、登录卡、状态刷新和侧栏切换。

### 影响

P2：关键恢复操作难以准确点击，无障碍与真机可用性下降。

### 建议修法

只在窄屏断点扩大这些操作的可点击高度/宽度，复用既有 token 和布局，不改变桌面视觉层级。

### 验证

- 390px 下目标控件可点击区域不小于 44×44px。
- 明暗主题无横向溢出，桌面尺寸不变。

### 处理记录

2026-08-12：仅在 ≤640px 扩大登录、刷新、重新检查、运行设置和侧栏开关触控区域。真实浏览器 390×844 明暗主题下 5 个控件均至少 44×44px，`clientWidth=scrollWidth=390`，控制台 0 warning/error；定向回归和两套生产构建通过。

<!-- issue-record:end WB-535 -->

---

<!-- issue-record:start {"id":"WB-536","number":536,"source":"WB-536-server-first-completion-matrix-drift.md","title":"Server-first 完成度矩阵滞后于已交付能力","severity":"P2","area":"misc","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-536"></a>

## WB-536 · Server-first 完成度矩阵滞后于已交付能力

<details>
<summary>原始元数据</summary>

```yaml
id: WB-536
title: Server-first 完成度矩阵滞后于已交付能力
severity: P2
area: misc
status: fixed
origin: 🏚 文档漂移
files:
  - docs/agentmate-server-first-架构设计.md:269
created: 2026-08-12
```

</details>

### 问题

架构文档仍把项目执行分析、Console 设备选路、Agent 计划字段更新和 WorkItem 自动执行写成部分已有或未实现，但 WB-501～WB-504 已交付对应能力。

### 触发场景

维护者按“当前完成度与缺口”规划下一阶段工作。

### 影响

P2：重复立项、错误评估产品边界，并使权威文档与真实实现冲突。

### 建议修法

依据已归档 issue 和当前 API/UI 更新矩阵与实施顺序，只保留仍真实存在的缺口。

### 验证

- 每个更新声明都能追溯到实现和归档 issue。
- 文档不再把 WB-501～WB-504 能力列为未实现。

### 处理记录

2026-08-12：完成度矩阵依据 WB-501～WB-505 更新：设备 readiness/选路、WorkItem 自动执行、项目执行分析和 Agent Sprint 计划工具均标记为已交付；实施顺序改为已完成轨迹与真实剩余的 Workspace 对话、企业同步/设备、生产 SaaS 和增量分析边界。

<!-- issue-record:end WB-536 -->

---

<!-- issue-record:start {"id":"WB-537","number":537,"source":"WB-537-session-restore-behavior-test.md","title":"会话恢复关键失败路径缺少可执行行为测试","severity":"P2","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-537"></a>

## WB-537 · 会话恢复关键失败路径缺少可执行行为测试

<details>
<summary>原始元数据</summary>

```yaml
id: WB-537
title: 会话恢复关键失败路径缺少可执行行为测试
severity: P2
area: frontend
status: fixed
origin: 🏚 测试缺口
files:
  - package.json:6
  - src/stores/chatStore.ts:141
created: 2026-08-12
```

</details>

### 问题

前端回归主要通过 Python 搜索源码文本，缺少能实际调用 Zustand action、模拟 API reject 并断言 promise/状态的行为测试；因此 `openSession` 静默吞错长期未被发现。

### 触发场景

重构会话加载、路由恢复或 API helper 后运行现有回归。

### 影响

P2：实现语句改名会产生假失败，真实异步语义回退又可能漏检。

### 建议修法

建立最小 TypeScript 前端行为测试入口，先覆盖 WB-533 的成功/失败语义，并纳入统一 regression 命令。

### 验证

- 测试真实执行 store action，不读取源码字符串。
- 修改为吞错实现时测试会失败。
- `pnpm test:regression` 同时运行行为测试、Python 回归和 TypeScript 检查。

### 处理记录

2026-08-12：引入 Vitest 最小行为测试入口并纳入 `pnpm test:regression`；测试真实执行 Zustand `openSession` 和 Console API 请求，不读取源码字符串，覆盖失败 reject/状态保持、成功提交、Server URL 校验及 HttpOnly Console transport。当前 3 个测试文件 6 项通过，TypeScript 全量门禁同步通过。

<!-- issue-record:end WB-537 -->

---

<!-- issue-record:start {"id":"WB-538","number":538,"source":"WB-538-sidecar-retired-business-api-surface.md","title":"兼容 sidecar 仍挂载已退役业务 API 面","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-538"></a>

## WB-538 · 兼容 sidecar 仍挂载已退役业务 API 面

<details>
<summary>原始元数据</summary>

```yaml
id: WB-538
title: 兼容 sidecar 仍挂载已退役业务 API 面
severity: P1
area: backend
status: fixed
origin: 🏚 迁移遗留
files:
  - backend/main.py:170
  - src-tauri/src/lib.rs:31
created: 2026-08-12
```

</details>

### 问题

WB-530 为恢复设备管理页面让正式 sidecar 启动完整兼容 app；该 app 除模型、技能、连接器、设置和诊断外，仍挂载项目、文件、会话等已迁往 Server 的业务 API，和 Local Agent 执行节点边界不一致。

### 触发场景

启动正式 Tauri sidecar，读取 OpenAPI 或请求退役的本地业务路由。

### 影响

P1：安装版继续携带第二业务权威面，匿名本机身份还能访问遗留数据，增加分叉与攻击面。

### 建议修法

建立明确的 Desktop compatibility app，只挂载 Local Agent Core 与仍保留的设备管理路由；退役业务路由不得出现在安装目标，同时保留纯本地开发模式的完整 app。

### 验证

- 安装目标保留模型、技能、连接器、运行设置、诊断、记忆与安全 API。
- 项目、文件、业务会话等退役路由返回 404。
- 纯本地显式模式仍可运行完整 app，不影响未登录离线开发。

### 处理记录

2026-08-12：新增安装专用 `compatibility_app` 白名单，保留模型、Run 工件文件、Skill、频道/ASR、WeKnora 本机知识源、连接器、设置、诊断、记忆、数据管理、安全策略和受 IPC 保护的 Core；项目、业务 Session/Run、聊天、任务、里程碑、自动化、通知、目录、灵感、OpenAPI/Docs 不再挂载。显式本地开发继续使用完整 app。Core/安装路由回归 9/9、Local Agent 全量 476 项、Tauri 3/3 通过。

<!-- issue-record:end WB-538 -->

---

<!-- issue-record:start {"id":"WB-539","number":539,"source":"WB-539-console-http-only-session.md","title":"Console 将长效 Bearer 令牌持久化在 localStorage","severity":"P1","area":"fullstack","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-539"></a>

## WB-539 · Console 将长效 Bearer 令牌持久化在 localStorage

<details>
<summary>原始元数据</summary>

```yaml
id: WB-539
title: Console 将长效 Bearer 令牌持久化在 localStorage
severity: P1
area: fullstack
status: fixed
origin: 🏚 既有缺陷
files:
  - console/src/api.ts:40
  - server/routers/auth.py:1
created: 2026-08-12
```

</details>

### 问题

Server Console 把可长期使用的 Bearer token 写入 `localStorage`，任何同源脚本执行漏洞都可直接读取并外带；登出也只清浏览器值，Server 没有 Console 专用 HttpOnly 会话边界。

### 触发场景

登录 Console 后检查存储，或在 Console 同源上下文执行脚本读取 `wb.console.token`。

### 影响

P1：账号、组织和项目管理权限可能在 XSS/供应链脚本事件中被持久窃取。

### 建议修法

保留 Desktop/App 的 Bearer API，同时为同源 Console 增加 Secure/HttpOnly/SameSite 会话 cookie、显式登出和 CSRF 防护；Console 不再持久化 token。

### 验证

- Console 登录后 localStorage/sessionStorage 均无 bearer token。
- 刷新后 cookie 会话仍可恢复，登出立即失效并清 cookie。
- 跨站写请求被拒绝，Desktop Bearer 客户端保持兼容。

### 处理记录

2026-08-12：Server 为显式 Console 请求签发 `HttpOnly; SameSite=Strict; Path=/api` cookie，HTTPS/生产环境加 Secure；cookie 鉴权写请求同时校验同源 Origin 与不可由表单伪造的 Console header，登出撤销 Server token 并清 cookie。Console 不再读写或发送 Bearer/localStorage，Desktop/App 的 Bearer 登录与验证保持兼容。cookie/CSRF 2/2、既有账号与 SSO 16/16、Console 行为测试/类型/构建通过；真实浏览器注册后刷新保持登录、登出立即回登录页且 0 warning/error。

<!-- issue-record:end WB-539 -->

---

<!-- issue-record:start {"id":"WB-540","number":540,"source":"WB-540-dompurify-security-advisories.md","title":"Markdown 清洗依赖 DOMPurify 命中 XSS 安全公告","severity":"P1","area":"frontend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-540"></a>

## WB-540 · Markdown 清洗依赖 DOMPurify 命中 XSS 安全公告

<details>
<summary>原始元数据</summary>

```yaml
id: WB-540
title: Markdown 清洗依赖 DOMPurify 命中 XSS 安全公告
severity: P1
area: frontend
status: fixed
origin: 🆕 依赖公告
files:
  - package.json:33
  - pnpm-lock.yaml:1
created: 2026-08-12
```

</details>

### 问题

生产依赖解析为 `dompurify@3.4.11`，命中 GHSA-55q2-fjhq-7xh7（moderate，`IN_PLACE` hook 相关 XSS）及 GHSA-c2j3-45gr-mqc4（low，自定义元素 hook 绕过）。AgentMate 用它清洗 Markdown/Skill 内容，属于直接安全边界。

### 触发场景

运行 `pnpm audit --prod`，或渲染攻击者可控的 Markdown/Skill 内容。

### 影响

P1：清洗器漏洞可能让恶意内容在 App/Console 中执行脚本并访问当前页面权限。

### 建议修法

升级到同时修复两个公告的 `dompurify>=3.4.13`，保留现有清洗策略并跑 Markdown、前端行为、类型与生产构建回归。

### 验证

- `pnpm audit --prod` 无生产依赖漏洞。
- Markdown/Skill 渲染与两套生产构建通过。

### 处理记录

2026-08-12：DOMPurify 升级至 3.4.13，修复 GHSA-55q2-fjhq-7xh7 与 GHSA-c2j3-45gr-mqc4；`pnpm audit --prod` 返回 0 漏洞，前端行为 6 项、Local Agent 回归 476 项、TypeScript 与 App/Console 生产构建通过，Console dist 与源码一致。

<!-- issue-record:end WB-540 -->

---

<!-- issue-record:start {"id":"WB-541","number":541,"source":"WB-541-sse-starlette-version-conflict.md","title":"MCP 传递依赖与 FastAPI Starlette 版本约束冲突","severity":"P2","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-541"></a>

## WB-541 · MCP 传递依赖与 FastAPI Starlette 版本约束冲突

<details>
<summary>原始元数据</summary>

```yaml
id: WB-541
title: MCP 传递依赖与 FastAPI Starlette 版本约束冲突
severity: P2
area: backend
status: fixed
origin: 🆕 依赖漂移
files:
  - backend/requirements.txt:8
created: 2026-08-12
```

</details>

### 问题

`mcp==1.2.0` 未锁定其传递依赖 `sse-starlette`，当前环境被解析为 3.4.5；该版本要求 `starlette>=0.49.1`，而 FastAPI 0.115.6 的项目约束固定为 0.41.3，`pip check` 失败。

### 触发场景

重新安装/升级 Python 环境后运行 `python -m pip check`，或触发依赖于不兼容 Starlette API 的 SSE 路径。

### 影响

P2：安装结果不可复现，运行时存在未被单元测试覆盖的 API 不兼容风险。

### 建议修法

显式固定仍支持 Starlette 0.41.3 的 `sse-starlette==2.1.3`；重新安装后运行 `pip check`、MCP/流式回归和完整后端测试。

### 验证

- `pip check` 无依赖冲突。
- MCP stdio、SSE 与完整 Local Agent/Server 回归通过。

### 处理记录

2026-08-12：在 `backend/requirements.txt` 显式固定 `sse-starlette==2.1.3`，与 FastAPI 0.115.6 / Starlette 0.41.3 一致；隔离环境安装后 `pip check` 返回无冲突，Local Agent 476 项、Server 192 项和 Python 全量编译通过。

<!-- issue-record:end WB-541 -->

---

<!-- issue-record:start {"id":"WB-542","number":542,"source":"WB-542-collaborative-asset-upload-actor.md","title":"协作成员无法上传既有项目资产且审计归错账号","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-542"></a>

## WB-542 · 协作成员无法上传既有项目资产且审计归错账号

<details>
<summary>原始元数据</summary>

```yaml
id: WB-542
title: 协作成员无法上传既有项目资产且审计归错账号
severity: P1
area: backend
status: fixed
origin: 🏚 既有缺陷
files:
  - server/asset_object_store.py:43
  - server/asset_object_store.py:93
  - server/routers/assets.py:56
created: 2026-08-12
```

</details>

### 问题

项目成员对资产拥有写权限并能成功创建上传会话，但对象存储把上传会话的 `owner_id` 固定成资产创建者。成员随后以自己的身份上传分片时查不到该会话并收到 404；上传审计也错误记录成资产创建者本人。

### 触发场景

项目 Owner 创建资产记录并授予 Member 写权限；Member 对该资产调用上传开始接口成功，再调用分片接口。

### 影响

P1：Server-first 协作中的成员资产交付链路中断，并产生错误审计主体，无法可靠追责实际上传者。

### 建议修法

显式区分资产所有者和本次上传操作者：上传会话按 actor 约束，资产版本继续归资产 owner；开始、续传、完成及审计全过程传递真实 actor，并增加成员上传回归。

### 验证

- Member 可对有写权限的既有资产完成开始、分片、完成和下载。
- 新版本仍归资产 owner，上传/完成审计 actor 为实际 Member。
- Viewer 与项目外账号仍不能写入。

### 处理记录

2026-08-12：上传会话改为显式绑定实际 `actor_id`，分片、续传、完成均以该操作者授权；对象版本仍保留资产 owner，上传开始与对象提交审计记录真实成员。成员/Viewer/外部账号回归 4/4、Server 全量 203 项通过；硬重启临时 Server 后，Owner 建资产、Member 完成真实 HTTP 分片与提交返回 200。

<!-- issue-record:end WB-542 -->

---

<!-- issue-record:start {"id":"WB-543","number":543,"source":"WB-543-concurrent-first-registration-admin.md","title":"并发首批注册可产生多个平台管理员","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-543"></a>

## WB-543 · 并发首批注册可产生多个平台管理员

<details>
<summary>原始元数据</summary>

```yaml
id: WB-543
title: 并发首批注册可产生多个平台管理员
severity: P1
area: backend
status: fixed
origin: 🏚 既有缺陷
files:
  - server/routers/auth.py:48
  - server/db.py:1119
created: 2026-08-12
```

</details>

### 问题

开放注册通过独立的 `count_accounts()` 查询判断首个账号，再另行插入账号和签发 token。两个并发首批请求可同时读到零账号并各自获得 `is_platform_admin=true`；同名并发注册还可能从预检查穿透为数据库 500。

### 触发场景

空数据库开启密码注册，同时发送两个不同用户名的注册请求；在首账号判断处同步并发。

### 影响

P1：未授权注册者可能在初始化竞态中取得平台管理员权限，破坏 Server 业务权威和账号治理边界。

### 建议修法

把名称唯一性、首账号判定、账号插入、审计与 token 签发放进同一个 `BEGIN IMMEDIATE` 写事务；路由把唯一性冲突稳定映射成 409，并增加并发不变量回归。

### 验证

- 空库并发注册任意数量账号时恰好一个平台管理员。
- 同名并发注册恰好一个成功，其余为 409，不出现 500。
- 注册 token、审计及既有 bootstrap 流程保持通过。

### 处理记录

2026-08-12：新增 `register_password_account` 原子事务，在同一个 `BEGIN IMMEDIATE` 内完成名称复检、首管理员选举、账号/审计/token 写入；路由统一把名称冲突映射为 409。三路并发注册、同名竞态、账号安全与 Server 全量 203 项通过；临时 Server 三路并发真实 HTTP 注册均为 200 且恰好一个管理员。

<!-- issue-record:end WB-543 -->

---

<!-- issue-record:start {"id":"WB-544","number":544,"source":"WB-544-login-rate-limit-key-bypass.md","title":"登录限流组合键可被轮换用户名或来源绕过","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-544"></a>

## WB-544 · 登录限流组合键可被轮换用户名或来源绕过

<details>
<summary>原始元数据</summary>

```yaml
id: WB-544
title: 登录限流组合键可被轮换用户名或来源绕过
severity: P1
area: backend
status: fixed
origin: 🏚 既有缺陷
files:
  - server/routers/auth.py:105
  - server/sso_store.py:260
created: 2026-08-12
```

</details>

### 问题

登录端点只使用 `login:{ip}:{username}` 组合桶。同一来源每次更换用户名，或针对同一账号不断更换来源时，都会获得新的计数桶，无法触发既定每分钟阈值。

### 触发场景

把阈值设为每分钟 3 次，从同一来源依次尝试 8 个不同用户名；8 次请求均返回凭据错误而非 429。

### 影响

P1：密码喷洒、凭据填充和分布式账号暴力尝试可绕过应用层防护，增加账号接管风险。

### 建议修法

按照登录防自动化边界分别检查 username 桶与来源 IP 桶，任一超限统一返回无细节的 429；增加同 IP 多账号、同账号多 IP 和原有单组合回归。

### 验证

- 同一 IP 轮换用户名达到阈值后返回 429。
- 同一用户名轮换 IP 达到阈值后返回 429。
- 阈值内合法登录和泛化错误响应保持兼容。

### 处理记录

2026-08-12：登录限流拆为独立 `login-ip` 与 `login-account` 两个桶，任一超限只返回通用 429；覆盖同来源轮换账号、同账号轮换来源和原有单账号边界。定向 13 项与 Server 全量 203 项通过；临时 Server 真实 HTTP 轮换 11 个用户名时第 11 次稳定返回 429。

<!-- issue-record:end WB-544 -->

---

<!-- issue-record:start {"id":"WB-545","number":545,"source":"WB-545-sso-first-account-admin-atomicity.md","title":"SSO 首账号注册可导致平台永久没有管理员","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-545"></a>

## WB-545 · SSO 首账号注册可导致平台永久没有管理员

<details>
<summary>原始元数据</summary>

```yaml
id: WB-545
title: SSO 首账号注册可导致平台永久没有管理员
severity: P1
area: backend
status: fixed
origin: 🏚 既有缺陷
files:
  - server/sso_store.py:360
  - server/db.py:1174
created: 2026-08-12
```

</details>

### 问题

SSO 自动注册始终写入 `is_platform_admin=0`。当它成为数据库首账号时，之后显式 bootstrap 会因“已有账号”拒绝，密码注册也只会创建普通账号，平台进入有账号但无管理员的不可恢复状态。注册前的身份与邮箱检查还位于写事务之外，不能守住并发自动注册边界。

### 触发场景

空数据库配置可用 SSO provider 与开放注册，让外部身份先于密码/bootstrap 完成回调；随后尝试 bootstrap。

### 影响

P1：平台管理 Console 和治理 API 永久失去可用管理员；并发 SSO 自动注册还可能绕过显式绑定所依赖的身份/邮箱复检。

### 建议修法

在 SSO 自动注册的 `BEGIN IMMEDIATE` 事务内重新检查身份和邮箱，并与密码注册一致地原子选举唯一首管理员；审计明确记录 SSO 首管理员来源。

### 验证

- 空库首个 SSO 注册账号成为唯一平台管理员。
- SSO 与密码注册并发时仍恰好一个首管理员。
- 已有账号后的 SSO 注册保持普通账号；并发重复身份/邮箱不能创建重复账号。

### 处理记录

2026-08-12：SSO 自动注册在写事务内重新检查外部身份与邮箱，并与密码注册共享“首账号唯一管理员”不变量；禁用的随机密码散列移到锁外，首管理员审计保留 provider。SSO 首/次账号、SSO×密码并发、同邮箱并发和既有 SSO/账号生命周期共 26 项通过，Server 全量 203 项通过。

<!-- issue-record:end WB-545 -->

---

<!-- issue-record:start {"id":"WB-546","number":546,"source":"WB-546-unbounded-public-request-body.md","title":"公共 webhook 与 relay 可在校验前无界缓冲请求体","severity":"P1","area":"backend","status":"fixed","created":"2026-08-12","archive_format":2} -->
<a id="wb-546"></a>

## WB-546 · 公共 webhook 与 relay 可在校验前无界缓冲请求体

<details>
<summary>原始元数据</summary>

```yaml
id: WB-546
title: 公共 webhook 与 relay 可在校验前无界缓冲请求体
severity: P1
area: backend
status: fixed
origin: 🏚 既有缺陷
files:
  - server/main.py:90
  - server/routers/automation_webhooks.py:100
  - server/routers/relay.py:90
  - server/routers/assets.py:81
created: 2026-08-12
```

</details>

### 问题

Webhook、relay 和资产分片虽在 `await request.body()` 后检查长度，但缺少 `Content-Length` 或使用分块传输时，Starlette 会先把完整请求体无界缓冲到内存，之后的 64 KiB/分片上限才生效。

### 触发场景

向无需账号鉴权的 webhook 或持服务凭据的 relay 发送无 `Content-Length` 的超大分块请求体；也可对资产分片端点使用相同方式。

### 影响

P1：攻击者可在签名、token 或对象协议拒绝请求前消耗 Server 大量内存，形成低成本拒绝服务。

### 建议修法

在 ASGI 入口按路由建立流式硬上限：读取过程中累计字节，一旦超过 webhook/relay 64 KiB 或资产配置分片上限立即返回 413；合法消息按原 ASGI 帧重放给路由，保留既有协议校验。

### 验证

- 无 `Content-Length` 的分块超限请求在下游路由执行前返回 413。
- 伪造偏小 `Content-Length` 仍按实际接收字节拒绝。
- 边界大小合法请求、SSE 和普通 JSON API 不受影响。

### 处理记录

2026-08-12：新增纯 ASGI `RawIngressBodyLimitMiddleware`，按实际接收帧在下游前限制 webhook/relay 64 KiB 与资产分片配置上限，声明长度和真实长度均检查，合法帧原样重放。分块、伪造长度、边界值及既有 ingress 回归 11/11、Server 全量 203 项通过；临时 Server 对无 Content-Length 的 65,537 字节 chunked webhook 在路由前返回 413。

<!-- issue-record:end WB-546 -->
