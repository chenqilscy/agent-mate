# AgentMate backend — Python 3.11+
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
cryptography>=44,<51  # WB-433: per-device Ed25519 identity; private key remains local
langfuse==4.14.1  # WB-230: optional, default-off LLM observability (manual httpx instrumentation)
python-dotenv==1.0.1
pydantic==2.10.5
mcp==1.2.0
# We use MCP's stdio CLIENT only (not its SSE server), so pin starlette to the
# FastAPI-compatible version — mcp's newer-starlette pin is for a feature we skip.
starlette==0.41.3
# 本地语音输入 ASR（WB-139）。CTranslate2 后端的 Whisper，CPU int8 秒级；自带 PyAV 解 webm/opus。
# 首次使用会下载模型到 ASR_MODEL_DIR（需联网一次）。可选：不用语音就不必装（端点会诚实 503）。
faster-whisper==1.1.1
requests>=2.31  # faster-whisper utils 依赖它下载模型，1.1.1 未自动拉，显式列出
# 本地记忆语义检索（WB-167 认知记忆 档二）。fastembed = ONNX runtime 的文本嵌入，无 torch、CPU 快、离线零成本，
# 默认 BAAI/bge-small-zh-v1.5（首用下载模型到缓存，需联网一次）。可选：不装则记忆退回档一「按强度排序」非语义模式。
fastembed>=0.3
# 本机办公交付工具（WB-243）：专用结构化生成 + 重新打开校验，不用 run_command 临时装包。
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
reportlab==4.4.10
pypdf==6.14.2
# 可复用系统 Edge/Chrome 的安全浏览器工具（WB-244）；不下载 Playwright 自带浏览器。
playwright==1.61.0
