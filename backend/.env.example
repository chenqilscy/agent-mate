# WorkBuddy backend configuration.
# Copy to backend/.env and fill in. The API key NEVER leaves the backend.

# OpenAI-compatible endpoint. Works with DeepSeek / GLM / Kimi / MiniMax / OpenAI etc.
LLM_API_KEY=
LLM_API_BASE=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# Optional Langfuse observability (WB-230). Disabled and metadata-only by default.
# Create project keys in your self-hosted/private Langfuse instance, then restart backend.
# LANGFUSE_ENABLED=1
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_BASE_URL=http://localhost:3000
# LANGFUSE_TRACING_ENVIRONMENT=development
# LANGFUSE_SAMPLE_RATE=1.0
# LANGFUSE_CAPTURE_CONTENT=0

# Optional: server bind
HOST=127.0.0.1
PORT=8101

# Optional: sandbox workspace root (default: backend/workspace)
# WORKBUDDY_WORKSPACE=

# Optional connector credentials (forwarded ONLY to that connector's subprocess).
# GitHub connector: a personal access token (github.com → Settings → Developer
# settings → Personal access tokens). Needs Node/npx on PATH to launch the server.
# GITHUB_TOKEN=ghp_xxx

# Telegram connector (built-in, no Node needed): a bot token from @BotFather.
# TELEGRAM_CHAT_ID is optional — a default target for send_message (a numeric
# chat id, or @channelusername). Discover a chat id via the get_updates tool
# after the other party messages your bot.
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# TELEGRAM_CHAT_ID=

# Telegram 助理外部渠道（WB-072）—— 让 bot 收到的消息驱动本机 agent，回复发回 Telegram。
# 用长轮询（getUpdates）实现，无需公网地址，契合 local-first。默认关：仅有上面的 bot
# token 不会自动开启，须显式置 1。安全：白名单 + /start 配对——设了 TELEGRAM_CHAT_ID 就
# 只认它；没设则第一个给 bot 发 /start 的会话自动绑定为主人并锁定，其余一律忽略。
# TELEGRAM_ASSISTANT=1

# 金山文档 connector (built-in, no Node needed — shells out to the separately
# installed kdocs-cli / 金山文档命令行工具). A WPS 云文档 token: get it via
# `kdocs-cli auth login` (then `kdocs-cli auth status` shows it), or leave this
# blank and rely on the CLI's own system-keychain login. Only ever read here —
# never sent to the frontend, never leaked into other connector processes.
# KDOCS_TOKEN=
