# AgentMate 黄金任务

`tasks.json` 是 WB-239 定义的 10 个稳定任务身份、阶段和能力依赖。各阶段实现 issue 只为自己负责的
validator 增加真实夹具与断言；不得因为尚未进入后续阶段而删除、改名或把任务标成假通过。

WB-243 首先落地 G01–G04 的本机二进制生成/重新打开校验；G05 由浏览器运行时 issue 接入，
G07–G10 分别由协作、可靠自动化和能力发布阶段接入。完整 R1/R2/R3/R4 gate 只有在对应 validator
拥有真实执行证据时才可宣称通过。
