# Frontend has no secrets. LLM keys live in backend/.env only.
# This file documents the single frontend-relevant override:
# VITE_API_BASE=/api    # override the API base if not using the Vite proxy
