# dependencies
node_modules/
backend/.venv/
backend/__pycache__/
**/__pycache__/

# build
dist/
*.tsbuildinfo

# env & secrets (API keys live in backend/.env only, never committed)
.env
backend/.env
# Local persistent test-account credentials (usernames may be shared; passwords must not be committed).
docs/local-test-accounts.md

# local data
backend/agentmate.db
backend/agentmate.db-shm
backend/agentmate.db-wal
backend/workspace/
!backend/workspace/.gitkeep
# 浏览器登录态/cookie（WB-244）：DB 同级、后端专用，绝不进 agent workspace 或版本库。
backend/.browser-profiles/
# 本地 ASR 模型缓存（WB-139，faster-whisper 首次使用下载，~145MB，不入库）
backend/models/
# 知识库文档字节落盘（WB-171，用户上传的团队资料，不入库）
server/storage/

# editor / os
.DS_Store
.vscode/
*.log

# test / tooling artifacts
.playwright-mcp/
/*.png
# sidecar smoke 测试产生的本地 DB（WB-419 配套：本地数据，不入库）
backend/.sidecar-smoke-langfuse/
# scratchpad 临时产物（截图、未提交构建产物；铁律：提交前清理）
scratchpad/
console/scratchpad/
